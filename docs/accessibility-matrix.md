# Accessibility verification matrix

## Automated contracts

- Every primary Lab is a named semantic region.
- Native buttons, range inputs, selects, details, and links provide keyboard operation without custom key handlers.
- The persistent navigation exposes Start, Piano, and Personal Lens directly, groups every supporting lab inside a native `details` disclosure, and names the conditional return-to-phrase region for assistive technology.
- Focus-visible outlines cover links, buttons, inputs, and selects.
- Dynamic audio and analysis status uses polite live regions.
- Resonance Sky is a named Piano subregion with one named SVG title and dynamic description plus a separate 280-millisecond-clustered atomic polite status. Its adjacent three-part reading and native `Read the sky` disclosure repeat every essential channel in text: fixed fifths direction, A4=440 register beacons, movable-Do hue, MIDI-attack size, all-twelve route membership and visits, leading and runner catalog fits, three fifths-tide scopes, exact semitone interval spans and reference line patterns, the twelve-point Semitone Horizon's sounding-pair counts, selected-scale gap sizes and concrete move-to-Do cue, interpreted and audible chord hulls, exact/incomplete and together/rolled boundaries, contextual-center cues, attack contour and grouped bouquets, detector relationship-window echo, low-change field possibilities, release-proven broken wake, pedal ring, selected-Do heuristic arrow, crunch ripple, and first/return bloom. Latest-attack/nearest-neighbor interval priority is repeated in visible text. Live held fields and latest attack-time snapshots receive different visible and announced copy. The SVG has no focusable marks; native Piano controls, compact assumed-spectrum select, visual-key disclosure, and optional silent-hand-horizon disclosure preserve ordinary focus order. Twenty-eight trail events, twelve contour events, eight field notes, twelve interval links, four chord membranes, and five collision-aware proximal annotations bound visual and announcement density; omitted annotations remain in the dynamic description or adjacent reading. Horizon counts use cyan circles, route gaps use violet diamonds, and the homeward cue uses a gold triangle, each with an explicit label and full-unit text alternative. Every hue has a label, position, seed, notch, shape, fill, outline, dash, ring, or line-style counterpart. Reduced motion removes only the one-shot attack bloom, forced colors remove gradients and filters while preserving the distinctions, and the 900- and 620-pixel layouts stack the proximal reading and guide without horizontal overflow in the 390-pixel immersive crop.
- The Piano Lab uses native buttons for every silent on-screen key, attack, learning lens, phrase-A/B intention/capture/replay/promotion/report, selected-attack reflection, Motion question, four-tap pulse action, performed-fingerprint and guided-scale action, tonal-gravity target and evidence-lane intervention, controlled-sonority recipe, interval-echo action, landmark replay/transposition, ghost target, chord-boundary correction, inherited-membership correction, and grouped chord gesture; native selects for MIDI device and chord timing granularity; text-redundant held, sustained, duration, pedal-tail, selected-attack provenance/attribution/guardrail, phrase-target/control/other-change/invariant/provenance, pulse-capture/phase/gap, performed-gap, gravity rank/component change, landmark changed/invariant/listen-for comparison, inherited, excluded, guided-step, controlled-baseline/replay, ghost, scale, chord-member, interval-source/replay/invariant, partial-alignment, interaction-zone, and voice-motion states; named visual summaries for the sixty-second phrase ribbon, selected-attack five-lens group, five-lens A/B profile, staff, A4=440 reference-frequency, fifths, performed octave positions, paired twelve-center gravity comparison, live pulse, source-versus-echo interval, two-note partial comb, landmark transposition, voice-leading, and evidence views; concise polite live explanations for the HUD, selected-attack attribution, phrase-B capture and one-change result, pulse capture, performed fingerprint, Interval Echo retry, gravity counterfactual, guided walk, landmark center change, and one-note sonority comparison; explicit text distinguishing measured MIDI key/timing data from derived 12-TET hertz coordinates; and double/dotted/solid outline, fill, line, circle, square, diamond, dash, cross, check, arrow, number, and text labels as well as color for selected-attack evidence provenance, A/B targets/invariants/changes/reports, baseline/intervention center bars, pulse lines and onsets, source-versus-echo hand shapes, partial alignment and interaction links, perceptual traces, voice strands, performed positions, walk progress, inherited exclusions, silent sonority references, ghost keys, and chord brackets.
- The persistent Piano route selector is a named region with a native select. It writes the selected route name, ordered semitone gaps, octave positions from Do, and locked-versus-following-evidence status in text. A visible three-item list distinguishes direct choice, performed-evidence discovery, and compatible-candidate adoption; adjacent copy states that the fifths compass changes Do while retaining the route. The 900-pixel contract places the method list on its own row, the 620-pixel contract stacks every field and list item, and forced colors preserve the selector boundary without relying on route color.
- That same persistent region contains a native pressed-state `Set Do from next note` action available in every Piano focus. Its polite status distinguishes idle, armed, and cancelable waiting states, says that both MIDI and silent on-screen attacks are accepted, and states that the current route locks. The resulting Do appears in the persistent state bar, re-labels the same physical key as Do, and persists in the shareable URL. At 320 pixels the 244-pixel action fits within the 282-pixel region and the document remains exactly 320 pixels wide; forced colors preserve the armed state with system highlight colors.
- The Immersion upper-right harmony lens appends first, second, or third inversion to an exact chord's bold name whenever the bass differs from the catalog root. The adjacent text redundantly names the realized bass, root, inversion, and quality; root position explicitly states that root and bass coincide. This reading does not depend on membrane color, weight, or spatial chord geometry, and the 320-pixel layout wraps without horizontal overflow.
- The Immersion lower-right Scale Lens is a named region with a complete image summary. Its twelve equal cells represent semitones from the learner-selected Do; route membership is repeated by cell fill and degree number, the recent five-attack window by dots, the latest position by a double outline, and live sounding state by an inset outline. The latest note, exact semitone position, route degree or outside-route status, cyclic gap loop, retained coverage, route name, and locked/discovery state are written beside the strip. The 900-pixel contract places it in the fourth edge-HUD quadrant, the 620-pixel contract stacks it in normal reading order, and forced colors preserve route, recent, latest, and sounding distinctions.
- The Scale Lens reset row uses two native buttons with explicit pressed states. Do reset changes its visible label to `Cancel Do reset` and a polite status says that the next new MIDI or on-screen attack becomes Do. Frame reset changes to `Restart frame · play scale`; its polite status counts distinct post-reset pitch classes from zero through the four-note minimum. Starting either workflow makes the other visibly inactive. At 320 pixels both 123.5-pixel actions fit the 252-pixel lens with equal client and scroll widths, and forced colors preserve the armed state with `Highlight`.
- After at least one attack, the Scale Lens adds a compact ascending fingering strip derived from the recent played octave. Every route position and octave closure has a visible finger digit and one complete nonvisual sequence; the hand, below/at-or-above-middle-C register rule, thumb/pinky endpoints, and crossing point remain written. Seven-, six-, and five-position routes use distinct route-count drills. The cue is explicitly advisory rather than an observed fingering, anatomical fit, fixed hand territory, or universal answer independent of black-key layout, direction, hand size, articulation, tempo, and surrounding music.
- Immersion note and chord history share one written twelve-attack boundary. The HUD state bar exposes the current count without relying on fading or membrane opacity, and the dynamic SVG description states that note marks and chord groups use the same window. The four visible chord membranes remain a drawing cap, not a separate or longer chord memory. Other Piano focuses continue to use the longer retained phrase.
- Research HUD is a separate named Piano focus that reuses that written MIDI/history state. Three native pressed-state layout buttons select the chromatic clock, thirds lattice, or all-pairs matrix; a native select changes the modular-generator overlay without moving the clock positions. The clock and lattice are named images with complete textual descriptions, while the matrix supplies written row and column headers plus every directed semitone value. Latest, sounding, recent, and route-member states use labels, outlines, shapes, and text in addition to color. Visible boundary copy states that a fifths path counts repeated `+7 (mod 12)` steps rather than affinity, consonance, function, or emotion, and that pitch-class views fold register. Forced colors preserve selected controls and structural lines; narrow layouts keep all twelve positions and the complete matrix inside the viewport without requiring hover.
- Scale Gravity HUD is a separate named Piano focus that reuses the shared live field and newest-twelve-attack history. Its thirteen image-labeled runway positions include every exact physical offset from selected Do through the octave endpoint; a separate seven-button native group pins a held degree with 44-pixel targets at phone width. Held degree, latest attack, sounding state, recent count, mode home, and one-semitone seams use text, position, count, outline, and marker differences in addition to color. Native IV, V, and I buttons expose the held pitch's interval, triad membership or every equally nearest triad tone in written form; `sounding now` and `last captured` triad matches remain explicitly distinct. A native select controls the local modal re-homing exercise. The three readings are explicitly labeled `Exact structure`, `Style-bound tendency`, and `Listening prompt`, and the major-scale cultural boundary remains visible. Forced colors preserve selected and structural states, while the 900- and 620-pixel layouts stack the runway, context cards, readings, and mode controls without requiring hover or custom keyboard handling; at 320 pixels essential labels remain at least 11 pixels and the document does not overflow.
- Its compact fingering guide is a named SVG image whose alternative text repeats the detected chord, register-based hand suggestion, and every note-to-finger assignment. Visible copy identifies left versus right hand, defines digits 1 and 5 as thumb and pinky, names the middle-C midpoint rule, and states that hand size, black keys, crossing, accompaniment, and the following chord can change the choice. Played keys retain both finger-number circles and highlighted key surfaces; forced colors preserve key outlines and numbered markers. At 320 pixels the 220-pixel keyboard fits within the 252-pixel harmony lens without document, region, graphic, or SVG overflow.
- Sight Shapes v2.13 is a named Piano focus with native chapter, exercise, exact-versus-transposable, chord-window, scaffold, and practice controls. Its eighteen curated exercises expose written score spelling, staff location, generic interval, signed semitone distance, contour, grouping, chord spacing, fixed points, and next landing in visible text as well as graphics; staff and keyboard views have text equivalents, and hue is redundant with numbers, labels, shapes, position, border, and line style. Dynamic comparison and diagnosis use a polite status region. Every Sight Shapes action remains at least 44 by 44 pixels, requires neither hover nor audio, and stays in normal keyboard order. Reduced-motion rules remove nonessential transitions, forced colors preserve selected and structural states, and production-browser checks at 1280, 390, and 320 pixels find no document overflow. These automated and browser contracts do not replace physical MIDI, VoiceOver/NVDA, high-contrast-display, touch-device, or switch-control testing.
- Sight Shapes v2.14 keeps the v2.13 controls and alternatives while making notation state explicit in text: the score states that it is one unbarred measure with no key signature, that accidentals carry to the closing bar, and that `♮` cancels an alteration. Contextual sharp/flat spelling is shared by score labels, shape solfège, destination copy, keyboard labels, and exact-match overlays rather than being communicated by staff position alone. A written-frame status names the frozen Do/route; if live discovery moves, a visible status and native `Adopt live frame` action prevent a silent target change. Frozen capture produces an alert with a native `Resume capture` action. Unordered chord packets expose all remaining members through multiple score and keyboard magnets plus written “attack order does not matter” guidance. On narrow screens, the notation and keyboard are separately named, keyboard-focusable horizontal regions that auto-center the next frame or destination; manual scrolling remains available, and reduced-motion preference replaces smooth auto-follow with immediate movement. These source and automated contracts still require physical MIDI, VoiceOver/NVDA, high-contrast-display, touch-device, switch-control, and responsive auto-follow verification on real devices.
- The Interval Context Echo is a named lesson with one replay question. Its source-versus-echo SVG has a complete text alternative; circles and a solid span distinguish the source from squares and a dashed echo. Five named lenses expose measured, modeled, selected-frame, and listener-only provenance in text. Sequential and release-unknown states explicitly explain why no simultaneous spectral comparison appears. All actions are native buttons, the interval network and partial microscope use a native disclosure, forced-color mode preserves source and echo distinctions, and the 900- and 620-pixel contracts reflow the five lenses from three columns to two and then one without horizontal scrolling.
- The Interval Experience Handoff uses a native `Reflect on source + echo` button and moves focus by normal document order into the existing named Personal Character Map. The specimen origin, exact four captured attacks, and local-only status are written in text. Each of four listener dimensions appears as one named question with a native five-button group; measured, modeled, and reported rows retain explicit provenance. At 390 pixels the toolbar, map, question, choices, and evidence rows fit a 352-pixel content column with no document overflow.
- The Chord Change Experience Handoff is one named before-and-after region with a labeled five-lens group. Sound, Relationships, Motion, Context, and Experience use visible provenance language, and the listener action is a native `Reflect on chord change` button that remains disabled until the two gestures contain at least three attacks. The exact captured-attack count, bounded origin, local-only status, and explicit whole-phrase exit are written in text. The four listener dimensions reuse native five-button question groups, the save action has chord-change-specific copy, Clear restores phrase mode, and the existing Personal Lens exposes export and deletion controls. At 390 pixels the 352-pixel panel and each 316-pixel lens fit without document overflow.
- Chords exposes three native pressed-state question buttons, with only the selected lesson rendered. Chord Voicing Echo uses circles for source notes, squares for the new voicing, text labels for every note, titled nearest-key strands, and a named SVG summary so structure never depends on color. Match, mismatch, bass-role change, provenance, model limits, and the listener-only boundary are written in text. Freeze, End, and Reflect are native buttons; reflection appears only after a structural match. Forced-color rules retain distinct source and attempt marks. At 390 pixels the question choices stack, the 352-pixel echo region and 316-pixel lens cards fit the document, and the primary action expands to the available width.
- The Chord Semitone Atlas is a named region with three simultaneously visible major, minor, and diminished articles. Each article has a complete SVG name and description, visible semitone offsets, adjacent and folded gap strings, three text rows breaking out fundamental and assumed-partial frequencies, and written down/nowhere/up voice-move explanations. The current exact type uses a top highlight plus the words `current exact type`; other cards say `reference type`. Voice color is redundant with separate lanes and labels, fundamentals differ from later partials by radius, and direction uses arrows plus words. Forced colors preserve axes, partial marks, fundamentals, and the current-card indicator. The 620-pixel contract stacks all three cards and keeps each frequency row within the available column.
- The standalone Voice lab has its own top-level navigation action, named region, native chromatic anchor selector, optional conventional-name checkbox, visible Start/Stop microphone and Hear target/interval buttons, and a labeled native −12 through +12 semitone selector. A native meter and adjacent text distinguish microphone input activity from accepted periodic pitch. The dynamic rail's complete screen-reader summary names estimated microphone hertz, nearest key, signed cents, coarse target semitones, fine cents, frequency difference, and detector periodicity clarity; below/at/above color never stands alone. Microphone permission and failure states use a polite status region. The 900-pixel contract moves the rail and third reference field to their own rows, the 620-pixel contract stacks reference controls, input evidence, readings, and actions, and forced colors preserve target and current markers.
- Chord Motion Echo is progressive content inside the existing Two chords question. Its freeze action, two-field progress status, retry, end, and conditional reflection are native controls with visible text. The named four-row SVG uses circles for the source, squares for the replay, open versus filled endpoint marks, solid versus dashed voice strands, per-mark titles, and a full textual summary. The five lenses repeat every decisive value and provenance boundary outside the diagram. A mismatch remains fully legible, including the distinct-endpoint-shifts case, and removes the reflection action rather than communicating failure through color. Forced-color rules preserve shapes and line patterns. At 390 pixels the 352-pixel region, five 316-pixel lenses, and two 316-pixel actions fit without document overflow.
- Tonal Frame Echo appears progressively only for a matched nonzero chord motion. Its two native buttons share a named group and expose mutually exclusive `aria-pressed` states. The question, fixed-versus-moved reference assumption, unchanged evidence, modeled outputs, and listener boundary are all visible text; role changes also update the replay marks' SVG titles. Forced-color mode supplies a highlighted pressed state. At 390 pixels the question reflows above a full-width 316-pixel two-button group inside the 352-pixel echo region without horizontal overflow.
- Octave Fold appears progressively only after a Chord Voicing Echo match. Its native toggle exposes `aria-pressed`; a later performed attempt resets the effective reveal. The named SVG has a full text description, visible gap values, circular source versus square replay marks, solid versus dashed segment styles, explicit closure marks, and per-node titles. The complete fingerprint, removed-doubling count, canonical-start caveat, and unchanged-evidence boundary are repeated outside the diagram. Forced colors preserve marks, paths, closure, pressed state, and the reading border. At 390 pixels the question, button, figure, and reading are each 316 pixels inside the 352-pixel echo region without document overflow.
- Gap Mutation reuses that same progressive native toggle and SVG region for a one-position mismatch. Signed `Δ−1`/`Δ+1` text makes changed gaps non-color-dependent; changed source and replay segments also use a thicker short-dash path, and the replaced circular/square positions receive a stronger outline. The accessible image summary names the displacement, both complete gap arrays, changed-gap count, and twelve-step closure. The text reading repeats the transformation and states that the retained anchor aligns geometry only. Forced colors map changed segments and nodes to `Highlight`. At 390 pixels the question, full-width action, figure, reading, and five lenses remain 316 pixels wide with no document overflow.
- Scale Landing Mutation uses the existing native Restart and End controls plus one native Change One Landing action after a completed route. Its named comparison region gives the two thirteen-position rails one complete text alternative; the source uses circles, the new route uses dashed squares, and a double outline marks the replaced positions. Every gap is labeled with its source value and either “held” or a signed visible delta. The polite status states the moved landing, changed-gap count, retained closure, and refusal reason. Forced colors preserve source, attempt, replacement, changed-gap, and reading-border distinctions. Free mutation mode outlines no target key. At 390 pixels the document has no horizontal overflow, the comparison rail stays 288 pixels wide, and seven gap cells reflow to four columns.
- Interval Ripple appears only after a successful one-key landing mutation. Two native buttons expose exclusive `aria-pressed` states and replace, rather than stack, the adjacent-gap and all-interval consequences. The ripple has a complete image summary; every retained endpoint is visible text, every source/new distance and multiplier is written, and shorter/wider direction never depends on color. Source bars are solid, new bars use dashed borders, and forced colors map them to `LinkText` and `Highlight`. The consequence reading is a polite live status and repeats changed and invariant relationship counts plus the absolute-frequency boundary. A wider move exposes neither toggle nor ripple. At 390 pixels the two choices stack and the 288-pixel spoke field fits without document overflow.
- The one-tone landmark counterfactual adds a named five-lens comparison, a live description of the single key replacement, native replay/restore/report buttons, pressed-state text for the listener choice, and explicit physical/model/listener provenance. Its 900- and 620-pixel layouts reflow from five columns to three and then one; forced-color mode preserves the intervention marker and selected report without relying on hue.
- The Live Phrase Landmark Bridge is a named region led by one explicit question. Its three-lane visual has a complete text alternative, labels the square live mark and circular curator mark redundantly, uses native return and landmark buttons, keeps advanced Atlas controls in a native disclosure, preserves system-color distinctions in forced-color mode, and stacks the lanes and actions at the declared 900- and 620-pixel breakpoints.
- The Pitchless Phrase Rhythm Bridge is a named region led by one explicit question. Its timing axis has a complete text alternative; onset groups use numbered marks and a distinct square shape when several attacks cluster; recurrence is written as “shape returns” as well as marked by a line; exact multiples, nearest ratios, overlap, silence, and missing release evidence remain text visible. Replay uses a native button, the authored cycle uses a native disclosure, forced-color mode preserves the onset and recurrence marks, and the 900- and 620-pixel contracts stack readouts, gap cells, copy, and actions without horizontal scrolling.
- The Live Phrase Expectation Trail is a named region led by one explicit question. Its event trail has a complete text alternative; no-precedent, seen-continuation, and new-continuation states use written labels plus dashed, solid, and double borders; a gesture return is written as well as underlined. The latest-event explanation states its evidence count in text, replay uses a native button, the generated journey uses a native disclosure, forced-color mode preserves known and new states, and the 820- and 560-pixel contracts reflow the reading, event cells, provenance copy, and action without horizontal scrolling.
- The Live Chord Change Bridge is a named region led by one explicit question. Its field transformation has a complete text alternative; the previous field uses circles and the current field uses squares; carried and moving voices use solid and dashed strands plus written motion labels. Every affordance change has a text value and circle-to-square rail markers. Five named lenses preserve measured, modeled, conditional, and unclaimed provenance; replay uses a native button; the generated Harmony instrument uses a native disclosure; forced-color mode preserves voice and affordance distinctions; and the 900- and 620-pixel contracts reflow the figure, affordances, lenses, and actions without horizontal scrolling.
- The Live Interval Assumption Bridge is a named region led by one explicit question. Its four-model comparison has a complete text alternative; circle, square, and diamond marks redundantly distinguish friction, harmonic fit, and fusion; direct values and per-model partial/interaction counts remain visible in text. Five named lenses preserve measured MIDI, derived A4=440 reference-frequency, modeled auditory, and listener-only provenance. Sequential and release-unknown states explain why no simultaneous result appears. Replay uses a native button, the generated Ear instrument uses a native disclosure, forced-color mode preserves metric and provenance distinctions, and the 980- and 680-pixel contracts reflow invariants, chart, model notes, lenses, and actions without horizontal scrolling.
- Canvas and dense visualizations provide accessible names or adjacent textual interpretations.
- Reduced-motion preferences disable smooth scrolling and collapse animation/transition duration.
- Color is redundant with text labels, position, marker shape, border style, or fill pattern. Atlas corpus clouds use dashed, dotted, and double borders plus distinct patterns.
- The first keyboard stop is a skip link to the active Lab, avoiding repeated traversal through the global lab selector and hero.
- Forced-color mode replaces decorative backgrounds with system colors, preserves selected states with `Highlight`, and distinguishes spectral voices through width and shape as well as color.

These contracts are tested in `tests/accessibility-contract.test.mjs` and source-rendering tests.

## Manual verification still required

The following must be checked with real assistive technology and are not claimed complete from source inspection:

- Full keyboard traversal and focus order in each Lab.
- VoiceOver and NVDA announcements for changing model states, sliders, plots, and local-file workflows.
- High-contrast and forced-color modes.
- Confirm system-color appearance in Windows High Contrast on physical displays; the automated forced-color contract does not substitute for this check.
- Touch target comfort and switch-control operation on real mobile devices.
- Physical MIDI-keyboard connection, sustain behavior, disconnect/reconnect announcements, and device switching with assistive technology.
- Reduced-motion behavior in a browser with the operating-system preference enabled.
