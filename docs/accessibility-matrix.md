# Accessibility verification matrix

## Automated contracts

- Every primary Lab is a named semantic region.
- Native buttons, range inputs, selects, details, and links provide keyboard operation without custom key handlers.
- The persistent navigation exposes Start, Piano, and Personal Lens directly, groups every supporting lab inside a native `details` disclosure, and names the conditional return-to-phrase region for assistive technology.
- Focus-visible outlines cover links, buttons, inputs, and selects.
- Dynamic audio and analysis status uses polite live regions.
- The Piano Lab uses native buttons for every silent on-screen key, attack, learning lens, phrase-A/B intention/capture/replay/promotion/report, selected-attack reflection, Motion question, four-tap pulse action, performed-fingerprint and guided-scale action, tonal-gravity target and evidence-lane intervention, controlled-sonority recipe, interval-echo action, landmark replay/transposition, ghost target, chord-boundary correction, inherited-membership correction, and grouped chord gesture; native selects for MIDI device and chord timing granularity; text-redundant held, sustained, duration, pedal-tail, selected-attack provenance/attribution/guardrail, phrase-target/control/other-change/invariant/provenance, pulse-capture/phase/gap, performed-gap, gravity rank/component change, landmark changed/invariant/listen-for comparison, inherited, excluded, guided-step, controlled-baseline/replay, ghost, scale, chord-member, interval-source/replay/invariant, partial-alignment, interaction-zone, and voice-motion states; named visual summaries for the sixty-second phrase ribbon, selected-attack five-lens group, five-lens A/B profile, staff, frequency, fifths, performed octave positions, paired twelve-center gravity comparison, live pulse, source-versus-echo interval, two-note partial comb, landmark transposition, voice-leading, and evidence views; concise polite live explanations for the HUD, selected-attack attribution, phrase-B capture and one-change result, pulse capture, performed fingerprint, Interval Echo retry, gravity counterfactual, guided walk, landmark center change, and one-note sonority comparison; and double/dotted/solid outline, fill, line, circle, square, diamond, dash, cross, check, arrow, number, and text labels as well as color for selected-attack evidence provenance, A/B targets/invariants/changes/reports, baseline/intervention center bars, pulse lines and onsets, source-versus-echo hand shapes, partial alignment and interaction links, perceptual traces, voice strands, performed positions, walk progress, inherited exclusions, silent sonority references, ghost keys, and chord brackets.
- The Interval Context Echo is a named lesson with one replay question. Its source-versus-echo SVG has a complete text alternative; circles and a solid span distinguish the source from squares and a dashed echo. Five named lenses expose measured, modeled, selected-frame, and listener-only provenance in text. Sequential and release-unknown states explicitly explain why no simultaneous spectral comparison appears. All actions are native buttons, the interval network and partial microscope use a native disclosure, forced-color mode preserves source and echo distinctions, and the 900- and 620-pixel contracts reflow the five lenses from three columns to two and then one without horizontal scrolling.
- The Interval Experience Handoff uses a native `Reflect on source + echo` button and moves focus by normal document order into the existing named Personal Character Map. The specimen origin, exact four captured attacks, and local-only status are written in text. Each of four listener dimensions appears as one named question with a native five-button group; measured, modeled, and reported rows retain explicit provenance. At 390 pixels the toolbar, map, question, choices, and evidence rows fit a 352-pixel content column with no document overflow.
- The Chord Change Experience Handoff is one named before-and-after region with a labeled five-lens group. Sound, Relationships, Motion, Context, and Experience use visible provenance language, and the listener action is a native `Reflect on chord change` button that remains disabled until the two gestures contain at least three attacks. The exact captured-attack count, bounded origin, local-only status, and explicit whole-phrase exit are written in text. The four listener dimensions reuse native five-button question groups, the save action has chord-change-specific copy, Clear restores phrase mode, and the existing Personal Lens exposes export and deletion controls. At 390 pixels the 352-pixel panel and each 316-pixel lens fit without document overflow.
- Chords exposes three native pressed-state question buttons, with only the selected lesson rendered. Chord Voicing Echo uses circles for source notes, squares for the new voicing, text labels for every note, titled nearest-key strands, and a named SVG summary so structure never depends on color. Match, mismatch, bass-role change, provenance, model limits, and the listener-only boundary are written in text. Freeze, End, and Reflect are native buttons; reflection appears only after a structural match. Forced-color rules retain distinct source and attempt marks. At 390 pixels the question choices stack, the 352-pixel echo region and 316-pixel lens cards fit the document, and the primary action expands to the available width.
- Chord Motion Echo is progressive content inside the existing Two chords question. Its freeze action, two-field progress status, retry, end, and conditional reflection are native controls with visible text. The named four-row SVG uses circles for the source, squares for the replay, open versus filled endpoint marks, solid versus dashed voice strands, per-mark titles, and a full textual summary. The five lenses repeat every decisive value and provenance boundary outside the diagram. A mismatch remains fully legible, including the distinct-endpoint-shifts case, and removes the reflection action rather than communicating failure through color. Forced-color rules preserve shapes and line patterns. At 390 pixels the 352-pixel region, five 316-pixel lenses, and two 316-pixel actions fit without document overflow.
- Tonal Frame Echo appears progressively only for a matched nonzero chord motion. Its two native buttons share a named group and expose mutually exclusive `aria-pressed` states. The question, fixed-versus-moved reference assumption, unchanged evidence, modeled outputs, and listener boundary are all visible text; role changes also update the replay marks' SVG titles. Forced-color mode supplies a highlighted pressed state. At 390 pixels the question reflows above a full-width 316-pixel two-button group inside the 352-pixel echo region without horizontal overflow.
- Octave Fold appears progressively only after a Chord Voicing Echo match. Its native toggle exposes `aria-pressed`; a later performed attempt resets the effective reveal. The named SVG has a full text description, visible gap values, circular source versus square replay marks, solid versus dashed segment styles, explicit closure marks, and per-node titles. The complete fingerprint, removed-doubling count, canonical-start caveat, and unchanged-evidence boundary are repeated outside the diagram. Forced colors preserve marks, paths, closure, pressed state, and the reading border. At 390 pixels the question, button, figure, and reading are each 316 pixels inside the 352-pixel echo region without document overflow.
- Gap Mutation reuses that same progressive native toggle and SVG region for a one-position mismatch. Signed `Δ−1`/`Δ+1` text makes changed gaps non-color-dependent; changed source and replay segments also use a thicker short-dash path, and the replaced circular/square positions receive a stronger outline. The accessible image summary names the displacement, both complete gap arrays, changed-gap count, and twelve-step closure. The text reading repeats the transformation and states that the retained anchor aligns geometry only. Forced colors map changed segments and nodes to `Highlight`. At 390 pixels the question, full-width action, figure, reading, and five lenses remain 316 pixels wide with no document overflow.
- Scale Landing Mutation uses the existing native Restart and End controls plus one native Change One Landing action after a completed route. Its named comparison region gives the two thirteen-position rails one complete text alternative; the source uses circles, the new route uses dashed squares, and a double outline marks the replaced positions. Every gap is labeled with its source value and either “held” or a signed visible delta. The polite status states the moved landing, changed-gap count, retained closure, and refusal reason. Forced colors preserve source, attempt, replacement, changed-gap, and reading-border distinctions. Free mutation mode outlines no target key. At 390 pixels the document has no horizontal overflow, the comparison rail stays 288 pixels wide, and seven gap cells reflow to four columns.
- The one-tone landmark counterfactual adds a named five-lens comparison, a live description of the single key replacement, native replay/restore/report buttons, pressed-state text for the listener choice, and explicit physical/model/listener provenance. Its 900- and 620-pixel layouts reflow from five columns to three and then one; forced-color mode preserves the intervention marker and selected report without relying on hue.
- The Live Phrase Landmark Bridge is a named region led by one explicit question. Its three-lane visual has a complete text alternative, labels the square live mark and circular curator mark redundantly, uses native return and landmark buttons, keeps advanced Atlas controls in a native disclosure, preserves system-color distinctions in forced-color mode, and stacks the lanes and actions at the declared 900- and 620-pixel breakpoints.
- The Pitchless Phrase Rhythm Bridge is a named region led by one explicit question. Its timing axis has a complete text alternative; onset groups use numbered marks and a distinct square shape when several attacks cluster; recurrence is written as “shape returns” as well as marked by a line; exact multiples, nearest ratios, overlap, silence, and missing release evidence remain text visible. Replay uses a native button, the authored cycle uses a native disclosure, forced-color mode preserves the onset and recurrence marks, and the 900- and 620-pixel contracts stack readouts, gap cells, copy, and actions without horizontal scrolling.
- The Live Phrase Expectation Trail is a named region led by one explicit question. Its event trail has a complete text alternative; no-precedent, seen-continuation, and new-continuation states use written labels plus dashed, solid, and double borders; a gesture return is written as well as underlined. The latest-event explanation states its evidence count in text, replay uses a native button, the generated journey uses a native disclosure, forced-color mode preserves known and new states, and the 820- and 560-pixel contracts reflow the reading, event cells, provenance copy, and action without horizontal scrolling.
- The Live Chord Change Bridge is a named region led by one explicit question. Its field transformation has a complete text alternative; the previous field uses circles and the current field uses squares; carried and moving voices use solid and dashed strands plus written motion labels. Every affordance change has a text value and circle-to-square rail markers. Five named lenses preserve measured, modeled, conditional, and unclaimed provenance; replay uses a native button; the generated Harmony instrument uses a native disclosure; forced-color mode preserves voice and affordance distinctions; and the 900- and 620-pixel contracts reflow the figure, affordances, lenses, and actions without horizontal scrolling.
- The Live Interval Assumption Bridge is a named region led by one explicit question. Its four-model comparison has a complete text alternative; circle, square, and diamond marks redundantly distinguish friction, harmonic fit, and fusion; direct values and per-model partial/interaction counts remain visible in text. Five named lenses preserve measured MIDI, modeled auditory, and listener-only provenance. Sequential and release-unknown states explain why no simultaneous result appears. Replay uses a native button, the generated Ear instrument uses a native disclosure, forced-color mode preserves metric and provenance distinctions, and the 980- and 680-pixel contracts reflow invariants, chart, model notes, lenses, and actions without horizontal scrolling.
- Canvas and dense visualizations provide accessible names or adjacent textual interpretations.
- Reduced-motion preferences disable smooth scrolling and collapse animation/transition duration.
- Color is redundant with text labels, position, marker shape, border style, or fill pattern. Atlas corpus clouds use dashed, dotted, and double borders plus distinct patterns.
- The first keyboard stop is a skip link to the active Lab, avoiding repeated traversal through the global lab selector and hero.
- Forced-color mode replaces decorative backgrounds with system colors, preserves selected states with `Highlight`, and distinguishes spectral voices through width and shape as well as color.

These contracts are tested in `tests/accessibility-contract.test.mjs` and source-rendering tests.

## Manual verification still required

The following must be checked with real assistive technology and are not claimed complete from source inspection:

- Full keyboard traversal and focus order in each Lab.
- VoiceOver and NVDA announcements for changing model states, sliders, plots, and local-file workflows.
- High-contrast and forced-color modes.
- Confirm system-color appearance in Windows High Contrast on physical displays; the automated forced-color contract does not substitute for this check.
- Touch target comfort and switch-control operation on real mobile devices.
- Physical MIDI-keyboard connection, sustain behavior, disconnect/reconnect announcements, and device switching with assistive technology.
- Reduced-motion behavior in a browser with the operating-system preference enabled.
