# Validation report

## Analytic fixtures

The automated suite checks waveform samples, normalized waveform sums, rational common periods, harmonic spectra, coincident partials, log-frequency distance, periodicity evidence from a known sine realization, pulse tempo, nested phase, recurrence, prediction information, and response-terrain behavior against closed-form or constructed answers.

## Published-style psychoacoustic condition

The Ear Lab roughness curve is a Plomp–Levelt/Sethares-style educational model. For equal-amplitude simple tones, the implementation has an analytic maximum separation

`ln(5.75 / 3.5) / ((5.75 - 3.5) × scale)`

where `scale = 0.24 / (0.021 × lowerHz + 19)`. Automated tests numerically recover that maximum at 250, 500, and 1000 Hz. This confirms the implementation follows its declared curve family and changes with absolute register.

This is **not** a reproduction of the original listener experiment. The project does not reproduce its participants, apparatus, calibrated levels, stimulus protocol, response scale, or statistical analysis. The check supports only the limited claim that the demonstrator has the intended critical-band-relative simple-tone behavior. See [research-ledger.md](./research-ledger.md) for the evidence anchor and prohibited claims.

## Sonority-affordance invariants

The sonority-model fixtures require all dimensions to remain separately bounded, a compact 4:5:6 field to show stronger harmonic fit and less ambiguity than a field containing √2, higher realizations to show greater spectral brightness, wider voicings to show greater openness, and recent motion to raise modeled activation while lowering modeled repose. These direction checks verify the declared explanatory logic; they do not validate an emotion prediction against listeners.

## Scale-learning invariants

The Scale Lab fixtures require every preset to close at an exact 2:1 octave, preserve its ratio structure under transposition, keep identical physical gaps in the same small/medium/large category, and spell the 3 · 2 · 2 · 3 · 2 path as Do · Me · Fa · Sol · Te in the declared chromatic movable-Do convention. Every inner-hearing path is authored for its specific scale, uses valid degree indices without modulo wrapping, varies the missing position, and avoids a target identical to either neighbor.

The browser review additionally checks that the model’s ten-partial harmonic spectrum is the spectrum synthesized for simultaneous examples, while melodic examples explicitly exclude simultaneous roughness as their explanation. Context and inner-hearing tasks collect a learner response before interpretation. These checks validate internal consistency and interaction behavior; they do not demonstrate learning gains in recruited participants.

## Piano and MIDI invariants

The piano-model fixtures pin MIDI note 69 to 440 Hz, require octave frequency doubling, verify that each learning scale closes after twelve equal keyboard steps, and preserve movable-Do role under transposition. Pairwise interval tests compare equal-key distances with declared simple-ratio landmarks without treating the landmark as the tuning identity.

The fifths fixtures generate the order Do → Sol → Re → La by repeated 3:2 multiplication and octave folding. They recover the approximately 1.955-cent difference between one pure fifth and seven equal-key steps, and the approximately 23.46-cent mismatch after twelve pure fifths versus seven octaves. MIDI parser fixtures cover note-on, explicit and zero-velocity note-off, channel preservation, and sustain-pedal thresholds.

Scale-candidate fixtures require a complete bright seven-position route to rank its matching Do frame at full compatibility while retaining other candidates. Tonal-tendency fixtures keep unresolved pull separate from home/arrival evidence, and sequential fixtures distinguish motion toward repose, away from repose, and small ambiguous changes. Source contracts also prohibit `AudioContext`, oscillators, and synthesizer activation inside Piano Lab so MIDI remains visualization-only.

The v1.26 HUD fixtures additionally require a strict seven-event note-on buffer that preserves repeated attacks, prevent the automatic scale frame from stabilizing before four distinct pitch classes, recognize chord inversions despite doubled notes, keep incomplete chord outlines explicit, and rank nearby scale chords by retained pitch classes with a concrete one-voice move when available.

The v1.27 temporal-chord fixtures enforce both adjacent-gap and maximum-span boundaries, keep inherited sounding tones out of attacked chord membership, reject repeated attacks of one pitch class as a chord, and independently measure pitch-set novelty, symmetric voice-motion distance, shared tones, and exact-root travel around the fifths cycle.

The v1.28 polish fixtures add bounded register-aware chord realization and reject empty, invalid, or overlarge pitch collections before combinatorial voicing work. Browser checks confirm that incomplete dyads remain labeled as outlines with an unknown root, exact triads restore root travel, suggested moves stay near the current hand position, essential HUD copy remains readable, and 390-pixel and desktop layouts avoid horizontal clipping.

The v1.29 phrase fixtures permit explicit break/join corrections at inferred chord boundaries while preserving automatic adjacent-gap and maximum-span behavior elsewhere. Source contracts require the live phrase ribbon, Interval Echo, causal chord view, prominent assumed-sound disclosure, tab-scoped session persistence, and active-lab URL state. Browser checks confirm five captured on-screen attacks persist across Scale/Piano navigation with the chosen learning lens, an interval can be matched from a new starting key, a forced join creates a corrected chord group, controls remain unclipped at 390 pixels, and no console errors are emitted.

The v1.30 phrase-memory fixtures retain attacks inside a rolling sixty-second boundary independently of the seven-event microscope. Voice-leading fixtures pin minimum-motion held, rising, falling, added, contrary, parallel, oblique, bass, and largest-leap results. Source contracts require a Voice-leading coach, silent ghost targets, URL-encoded Piano lenses, and the absence of the former answer-entering chord action.

The v1.31 scale fixtures preserve cyclic gap inventory and twelve-step octave closure under a rotated starting point. Performed-gravity fixtures require the sounded Do candidate to outrank compatible alternatives when it is repeated, longer, stronger, low, and phrase-final while exposing every component separately. Resolution-fork fixtures require unique pitch-class alternatives and a center-return and fifths-neighbor path without entering a note. Browser review confirms that the eighth attack no longer discards the first attack’s frame evidence, a chosen fork keeps its original intention label after the learner plays it, the target is silent and matchable in any octave, the Scales view has no warnings or horizontal clipping at desktop and 390 pixels, and the active Piano lens remains URL-addressable.

The v1.32 gesture fixtures separately pin detached silence, edge-connected release, finger overlap, pedal-mediated overlap, phrase ending, and still-held contact. Motif fixtures distinguish exact repetition, constant transposition, normalized-rhythm variation, altered endings, and a repeat after intervening attacks in non-overlapping three- or four-attack windows. Browser review performs an exact C–D–E recurrence and a deliberately overlapped two-note touch, confirms the expected labels and follow-up prompt, verifies no horizontal clipping at 390 pixels, and keeps measured articulation language separate from modeled motif recurrence. These checks establish deterministic teaching behavior, not notation recovery, phrase-boundary accuracy, or listener-validated motif salience.

The v1.33 landmark fixtures require four declared generated archetypes, valid three- or four-position chord fields, relationship-preserving transposition, stable bounded voicings, exact any-octave pitch-class matching, and inspectable shared-tone, keyboard-motion, largest-leap, and fifths-travel transition evidence. Browser review completes all four fields of the pop-loop path using only the silent on-screen keyboard, confirms automatic single-step advancement and completion, switches to the pedal-point path without entering notes, fixes Do for the duration of the exercise, and verifies readable native controls with no horizontal overflow at 390 pixels. These checks validate deterministic practice behavior and provenance copy; they do not establish that an archetype represents every work in a genre, evokes a universal response, or improves learning without a participant study.

The v1.34 Personal Character fixtures require a phrase relationship signature to remain unchanged under transposition and uniform tempo scaling, require summary uncertainty to decrease with additional observations, and reject malformed local ratings. Source contracts require four distinct reported dimensions, visible sample uncertainty, separate measured/model/reported evidence lanes, same-signature highlighting, and Personal Lens integration. Browser review captures a three-attack phrase, completes all four questions one at a time, saves a 75/75 map point, confirms persistence across reload, confirms the Personal Lens summary and enabled export/delete controls, and finds no console warnings or horizontal overflow at 390 pixels. These checks validate interaction, storage, and representational separation; they do not validate the wording as a psychometric instrument, infer emotion or preference, establish causation, or demonstrate learning gains.

The v1.35 assumed-spectrum fixtures declare four bounded teaching profiles, preserve the supplied base frequency under each profile—including normalized string-stretch models—and require a fixed set of reference frequencies to produce different spectral roughness and brightness evidence under sine versus bright-piano assumptions. Source contracts require a native model selector, visible partial comb, explicit “changes” and “stays fixed” copy, session persistence, and the continued absence of audio synthesis or capture in Piano Lab. These are model-sensitivity and interface checks, not evidence that either piano proxy matches a particular instrument or listener.

The v1.36 fifths fixtures generate thirteen cumulative positions including the attempted return, pin one pure fifth to `1200 × log2(3/2)` cents, recover the approximately 23.46-cent pure closure mismatch, require an intermediate temperament to reduce but not erase that mismatch, and require the 700-cent endpoint to close at zero. Source and accessibility contracts require step and temperament range controls, a named spiral graphic, an interactive compass, explicit invariant/change copy, and URL-addressable `pianoDo`/`pianoScale` frame state. Browser review morphs the live diagram from 701.955 to 700.000 cents per fifth, selects a spiral position and a compass position as movable Do without adding phrase attacks, confirms the coordinated frame and URL update, and finds no horizontal overflow at 390 pixels or browser warnings. These checks verify the arithmetic and interaction, not tuning preference, cultural universality, or learning gains.

The v1.37 membership fixtures require attacked notes to remain included, accept exclusions only for inherited sounding MIDI notes, prove that a correction never mutates the gesture’s complete sounding field, and propagate the corrected set into transition novelty and voice-leading context. Source and accessibility contracts require a named native-button correction group, reversible exclude/restore labels, persistent membership state, explicit “reading changes · sound stays” copy, and redundant crossed-node styling. Browser review holds Do across a later Mi–Sol attack cluster, confirms the automatic Do-major reading, excludes inherited Do, and observes a Mi-minor outline, a changed toward-Do value, two interpreted versus three sounding notes, and one crossed texture node while the three audible nodes and roughness value of 16 remain unchanged. Reload preserves the exclusion; restore recovers the exact Do-major reading. At 390 pixels the document width remains 390 with no horizontal overflow or browser warnings. These checks validate deterministic state propagation and evidence separation, not the learner’s harmonic intention or the musical correctness of either reading.

The v1.38 scale-walk fixtures require the same declared route to start from Do in multiple MIDI octaves, preserve exact route offsets through an upper Do, retain the expected next position after a wrong gap, expose actual and expected motion, and restart from a new-octave Do without changing the route. Source and accessibility contracts require native start/restart/end controls, a named polite live status, check/arrow/text-redundant progress, one exact silent keyboard target, tab persistence, explicit no-sound copy, and separate frequency, interval, fifths, and contextual-center descriptions. Browser review starts at middle Do, advances to Re, deliberately plays the intervening chromatic key, confirms that two positions remain complete and Mi remains the target, restarts from the next-octave Do, then completes Do–Re–Mi–Fa–Sol–La–Ti–Do and observes the 2:1 explanation. Reload preserves completion. At 390 pixels the live physics rows stack without horizontal overflow, and no browser warnings are emitted. These checks validate deterministic retrieval feedback and representational coordination, not fingering, tempo, expression, listener hearing, tonal truth, or learning gains.

The v1.39 controlled-sonority fixtures declare four relation-defined starting fields, isolate one added or removed MIDI note, attribute only its gained or lost pairwise intervals, and refuse a changed-note result when several notes differ. Source and accessibility contracts require native silent recipe controls, an exact-register target, a named polite live comparison, separate physical/auditory/contextual/conditional lanes, explicit spectrum-assumption and no-quality language, a multiple-change guardrail, tab persistence, and redundant silent-key styling. Browser review performs Do–Mi–Sol, adds Fa, observes three newly created relationships and four separate consequence lanes, removes more than one baseline member and confirms that all causal lanes disappear, then changes the teaching spectrum while physical and contextual facts stay fixed. Releasing the whole field enters an explicit replay phase; after only Do is restored, Mi and Sol remain outlined and no one-note removal is claimed. Completing the exact field resumes comparison. Reload restores the recipe and baseline as replay-required. At 390 and 1280 pixels the panel and document have no horizontal overflow. These checks validate deterministic attribution and state boundaries, not the actual spectrum of a keyboard or DAW, listener experience, harmonic function, emotion, consonance, quality, or learning gains.

The v1.40 pulse fixtures capture four repetitions of the first post-arm key, ignore other keys during capture, derive the median of three bounded tap gaps, preserve raw mean absolute spread, cluster later attacks inside a non-chaining 70 ms onset window, place clusters by phase around the declared pulse, compare consecutive cluster gaps with explicit ratio landmarks, and reject anchors containing gaps below 180 ms or above 2 seconds. Source and accessibility contracts require native Motion-question and pulse controls, a named polite capture status, a named rolling plot, text-redundant pulse/halfway/onset encodings, explicit no-sound and no-quality language, URL and tab persistence, source-tap expiry, and forced-color onset marks. Browser review establishes a four-tap Do anchor, observes the raw millisecond and per-minute coordinate, performs three later notes, receives phase and spacing-ratio readings, switches among Pulse, Touch, Voices, and Motif while confirming that unrelated coaches disappear, reloads into the selected question, returns to the preserved pulse, and confirms zero document or panel overflow at 390 and 1280 pixels. These checks validate deterministic timing coordinates and focus-state boundaries, not MIDI-device latency, meter, notation, swing, groove, timing accuracy, tempo following, listener entrainment, technique, quality, or learning gains.

The v1.41 performed-fingerprint fixtures build the same ordered gap route from two arbitrary MIDI origins, close only at twelve equal steps, preserve accepted positions after descending and overshooting attacks, repair a wrong replay gap without resetting the valid prefix, and distinguish exact catalog orientation from cyclic rotation. Source and accessibility contracts require an explicitly armed learner-authored exercise, a named octave-position visual, a polite one-relationship status, native transpose/rotate/reveal controls, delayed catalog translation, tab persistence, silent replay-key styling, and text-redundant accepted positions and gaps. Browser review authors `2–2–1–2–2–2–1` on the on-screen keyboard, confirms 2:1 closure before names appear, reveals the optional catalog translation, begins the same route from a different key, deliberately moves `+1` where `+2` is expected, observes that the valid prefix remains, repairs the move, and confirms that unrelated scale panels disappear during the active question. Reload preserves the replay exercise and its accepted gap. The document and builder remain exactly within 390- and 1280-pixel widths with no browser warnings. These checks validate deterministic relationship capture, retrieval, and focus boundaries—not fingering, fluency, intonation, cultural importance, tonal-center perception, emotional meaning, musical quality, or learning gains.

The v1.42 tonal-gravity counterfactual fixtures require a frozen phrase to retain all twelve baseline candidates, replace exactly one chosen contextual component across those candidates, preserve route fit and the other four component values bit-for-bit, recompute the target score from the declared lane weight, normalize pitch-class targets, and reject empty specimens. Source and accessibility contracts require native target/cue controls, a named paired twelve-center visual, outline-versus-fill redundancy, a polite exact rank/component reading, explicit invariant lanes, tab persistence, and the absence of key-confidence, tension, liking, or quality claims. Browser review performs Do–Mi–Sol–Do, recaptures the frozen phrase, and confirms that the default ending intervention moves Mi from center rank three to two while every MIDI event and other component stays fixed. Changing to held time produces the same auditable one-lane structure. Reload preserves the frozen specimen, target, cue, and URL-locked frame; unrelated scale diagnostics remain hidden during the question. The document, field, chart, and native controls remain within 390- and 1280-pixel widths, and browser diagnostics contain no warnings or errors. These checks validate deterministic heuristic sensitivity and state boundaries, not a new performance, a heard tonal center, listener expectation, cue interaction, causation, preference, emotion, correctness, musical quality, or learning gains.

The v1.43 phrase-comparison fixtures require two specimens of at least three attacks, preserve signed interval paths under uniform transposition, preserve normalized onset shape under uniform time scaling, expose the elapsed-time ratio separately, retain register and MIDI-attack changes, compare sounding-overlap share independently, rerank contextual center hypotheses, and refuse undersized specimens. Source and accessibility contracts require a named A/B entry, native capture/replay/promote/report controls, a five-lens group, text-redundant changed/invariant labels, distinct measured/model/reported provenance, tab persistence, forced-color border styles, and no aggregate similarity or goodness field. Browser review performs Do–Mi–Sol–Do, freezes A, replays the same signed path ten keys lower and more slowly, and observes changed sound, invariant relationships, changed motion, changed modeled context, and an initially unfilled experience lane. The learner reports B as more settled, equal energy, and A as liked more; no other lane changes. Reload preserves both frozen phrases, all three reports, and the URL-locked Do/scale frame. Unrelated chord and Explore diagnostics stay hidden while the comparison is active. The field, five lanes, report controls, and document remain within 390- and 1280-pixel widths, and browser diagnostics contain no warnings or errors. These checks validate deterministic relationship/timing invariance, provenance boundaries, and state persistence—not audio similarity, phrase alignment, intention, causal explanation, heard tonality, emotion inference, musicality, quality, preference generalization, or learning gains.

The v1.44 navigation fixtures accept only a bounded, structurally valid tab-scoped phrase summary and fall back safely for an older or unknown lens. Source contracts require a named native disclosure for supporting labs and a named return-to-phrase region. Browser review enters Piano from a URL-locked chord lens and movable-Do frame, performs Do–Mi–Sol, opens the grouped learning menu, visits Harmony, and observes a three-attack chord-lens return cue. Returning restores all three attacks and the full Piano URL state. The primary companion choices, three specialist groups, disclosure, and return cue remain within 390- and 1280-pixel widths, and browser diagnostics contain no warnings or errors. These checks validate deterministic navigation continuity, session parsing, keyboard-native structure, and responsive layout—not comprehension, learning gains, musical quality, or the suitability of one navigation hierarchy for every learner.

The v1.45 one-change fixtures distinguish the target observation, intention-specific control, changed lenses, invariant lenses, and other changed lenses for uniform transposition, altered timing, and a changed ending. Source contracts require a native six-choice intention group, a polite result status, explicit control language, text-redundant lens states, and forced-color target states. Browser review performs Do–Mi–Sol, declares a whole-phrase move, replays Re–Fi–La, and observes a +2-key register change, preserved +4/+3 signed path, invariant relationships and motion, and a separately changed modeled context. The result never calls this successful or causal. The choice list stacks into six full-width native buttons at 390 pixels; the completed reading remains within 390- and 1280-pixel documents, and browser diagnostics contain no warnings or errors. These checks validate deterministic thresholding, control logic, persistence shape, and responsive interaction—not intention recognition, execution quality, actual instrument audio, causal explanation, listener response, musicality, or learning gains.

The v1.46 partial-interaction fixtures require a 200:300 Hz exact-harmonic pair to expose a 3↔2 aligned partial and more alignment and overlap than 200:283 Hz, keep the sine model to one component per key, bound the roughness proxy, and reject invalid fundamentals. Source and accessibility contracts require a named two-comb visual, direct per-mark summaries, circle/solid versus diamond/dashed redundancy, explicit assumed-spectrum and no-audio language, separate alignment/interaction/roughness/overlap outputs, and forced-color fallbacks. Browser review plays Do–Sol: the exact-harmonic model exposes three aligned pairs including 3↔2 at about two cents, while sine shows no aligned or displayed interaction links and bright-piano shows four aligned plus six displayed interaction-zone links without changing either fundamental. Adding Mi makes the focused event pair with its nearest of three sounding notes and updates the reading to the compact 6:5 region. The microscope, plot, and readout remain within 390-pixel and 1280-pixel documents, reflow from one to four readout columns, and emit no browser warnings or errors. These checks validate deterministic model sensitivity, transparent pair selection, and responsive rendering—not actual keyboard or DAW audio, calibrated audibility, consonance, fusion, preference, emotion, musicality, goodness, or learning gains.

The v1.47 Interval Echo fixtures require a seven-step source moved one octave higher to preserve distance, direction, and a uniform +12-key shift while doubling its absolute hertz gap; they distinguish a reversed equal-span replay, reject a six-step mismatch, and reject invalid note input. Source and accessibility contracts require a named source-versus-echo visual, circle-versus-square and solid-versus-dashed redundancy, separate invariant/physical/spectral readings, explicit MIDI-spectrum limits, and forced-color fallbacks. Browser review freezes Do–Sol, replays Re–La, and observes the invariant seven-step 1.498:1 relationship beside a +2-key center shift, a 130.4-to-146.3 Hz gap, and harmonic-model roughness changing from 6 to 4 while overlap remains 19. A later Mi–Fa pair is treated as one independent retry, reports one rather than seven steps, retains the original source, and updates the physical and modeled coordinates without awarding a score. The panel fits its 484-pixel desktop column without internal overflow; the established 900-pixel breakpoint stacks the entire interval-practice grid for narrow layouts. These checks validate deterministic pairing, transposition arithmetic, source retention, and layout contracts—not actual audio, calibrated perception, fingering, intonation, correctness, similarity of experience, musicality, goodness, or learning gains.

The v1.48 selected-attack view reuses the tested controlled-sonority set-difference invariant: exactly one added note exposes only its relationships to retained notes, while multi-note changes cannot receive a changed-note result. Source and accessibility contracts require all five named lenses, visible measured/modeled/listener provenance, a polite attribution reading, text-redundant causal guardrails, a listener-reflection action, forced-color provenance borders, and no aggregate score. Browser review plays Do then Mi and selects attack two: Mi is the sole added member, its four-step compact 5:4-region relationship is attributable, measured motion is +4 keys, and modeled pull changes from 0 to 14 while home evidence remains 80. Selecting attack one reports no earlier microscope snapshot and avoids a fabricated before value. Releasing Do before attacking Sol produces one entry plus one exit; the view refuses a one-note cause while still reporting the measured melodic move and separate context-model deltas. The reflection action preserves the three-attack phrase and opens the first listener question. At 1280 pixels the five 221-pixel lanes and 1,108-pixel strip fit a 1,210-pixel panel without internal or document overflow; the declared 900- and 620-pixel contracts reflow to two and one columns. Browser diagnostics contain no warnings or errors. These checks validate attribution boundaries, selected-event coordination, provenance labels, navigation continuity, and layout contracts—not acoustic loudness, causation across lenses, listener response, emotion, correctness, musicality, goodness, or learning gains.

The v1.49 landmark fixtures transpose every pop-loop field from Do to its clockwise fifths neighbor by seven equal-key steps while proving that role order, root offsets, and within-field pitch offsets remain byte-for-byte unchanged. Session state is versioned and validates both centers and the generated path identity. Browser review completes the three-field cadence, moves the route to the fifths neighbor, confirms the new center and field-one reset, and completes the route again to receive the two-center comparison. Direct compass center changes use the same reset instead of carrying stale progress. Responsive contracts collapse the four-lane comparison from four columns to two and then one; its native controls, named status, text labels, and forced-color border do not depend on hue. These checks validate deterministic transposition and learning-state boundaries—not actual audio, identical hearing, style identity, emotion, correctness, musicality, goodness, or learning gains.

The v1.50 landmark fixtures run all four authored counterfactuals and require exactly one flattened MIDI target to differ from the source path, with every non-intervention field byte-for-byte identical. The profile must contain one source note, one target note, all retained notes, a nonzero signed key move, and before/after interval distances. Browser review performs the generated ii–V7–I cadence, starts “soften the leading pull,” and confirms that only the middle field changes from Ti to Li by one descending key while the preparation and arrival targets stay exact. The five-lens strip reports interval distances `4 · 6 · 9 → 3 · 7 · 8`, harmonic-model crunch `0`, repose `-3`, selected-Do pull `-12`, and home evidence `-6`; these are model outputs, not quality claims. After a complete replay, the separate “Original” listener report becomes pressed and survives reload with the version-15 tab session. Browser diagnostics contain no warnings or errors. At a 390-pixel viewport the comparison is one 316-pixel column, its action row stays within 287 pixels, and the document width remains 390 pixels. These checks validate the declared intervention, progress state, provenance separation, persistence, and responsive layout—not actual sound, causal hearing effects, emotion, style identity, preference, correctness, goodness, or learning gains.

The v1.51 Atlas fixtures validate bounded phrase parsing, chronological projection, invalid velocity and release rejection, transposition-invariant relationship recurrence and piece-local surprise, tempo-sensitive timing activity, and absent rather than manufactured evidence for short or simultaneous specimens. Browser review first confirms the empty bridge and closed advanced-controls disclosure, then enters Piano, performs six silent on-screen attacks, and returns to Atlas. The bridge exposes three provenance-labeled lanes for the retained phrase. Selecting *Blinding Lights* after *bad guy* changes the curator landmark and leaves every live phrase value unchanged, demonstrating that this is a juxtaposition rather than a classifier. The 1280-pixel document has no horizontal overflow and no visible runtime failure. Responsive source contracts reflow the bridge at 900 and 620 pixels; a physical narrow-device browser check remains outstanding because the current in-app viewport override did not apply. These checks validate deterministic local models, state continuity, selection independence, evidence absence, and provenance boundaries—not audio similarity, song matching, style identity, emotion, listener response, causal explanation, musicality, preference, goodness, or learning gains.

The v1.52 Rhythm fixtures validate pitch removal, 70 ms onset clustering, a deterministic median-gap ruler, exact normalized multiples, nearest simple-ratio coordinates, recurrence based on close actual lengths rather than shared clamped labels, velocity range, release-based connection classes, transposition invariance, proportional-tempo invariance, and refusal when timing evidence is insufficient. Browser review begins from the empty Rhythm bridge, hands off to Piano, performs five silent attacks with a deliberately longer second gap, and returns to a five-group pitchless profile. It reports a 586 ms local unit, displayed 1.00/1.51/1.00/1.00 normalized gap shapes, nearby 1:1 and 3:2 coordinates, and no invented release evidence. Returning through Piano safely closes the restored on-screen notes; Rhythm then reports four known connections without changing the onset fingerprint. The authored cycle remains closed by default. At 1280 pixels the 1,210-pixel bridge and 1,082-pixel gap row fit without document overflow. At a 390-pixel viewport the document remains 390 pixels wide, readouts stack, gap cells form two 154-pixel columns, all actions fit, and browser diagnostics contain no warnings or errors. These checks validate deterministic local timing geometry, grouping, evidence absence, tab continuity, and responsive layout—not a heard beat, intended meter, written duration, articulation intention, groove, swing, timing accuracy, listener response, emotion, correctness, musicality, or goodness.

The v1.53 Journey fixtures validate 70 ms field grouping, transposition-invariant pitch shape and bass motion, proportional-time invariance, incremental use of earlier transitions, exact known/new continuation states, inspectable alternative counts, and refusal for a single chord or a phrase of unrepeated contexts. Browser review begins from the empty Journey bridge, hands off to Piano, and performs six silent attacks with the movement pattern +2, +2, −1, +2, −1 keys. Returning to Journey exposes six onset groups: group four is a new continuation after earlier evidence, while group six is one of two learned continuations with 50% of the phrase-local evidence. Large browser-operation delays changed the displayed relative-time values but not the gesture evidence because timing remains a separate lane. The generated journey remains closed by default. At 1280 pixels the document remains 1280 pixels wide with no overflow or browser warnings. At 390 pixels the document remains 390 pixels wide, event cells form two 154-pixel columns, the latest-event reading and provenance copy stack, the action fits, and the disclosure remains closed. These checks validate deterministic local memory, evidence provenance, state continuity, and responsive layout—not a listener's expectation or surprise, detected form, phrase intention, style, emotion, correctness, musicality, preference, goodness, or learning gains.

The v1.54 Harmony fixtures require Do–Mi–Sol becoming Do–Me–Sol to preserve the two compact bass-relative shapes `0 · 4 · 7 → 0 · 3 · 7`, report one equal-key step of oblique nearest-key voice motion, and expose four independent conditional affordance deltas. Octave transposition preserves relationship shape and voice motion while allowing register-dependent modeled brightness to change. The same fixtures apply 80, 160, and 320 ms grouping plus explicit break/join corrections, reject invalid session corrections, and refuse to manufacture harmony from a separated melody or octave duplicates of one pitch class. Browser review begins from the empty Harmony bridge, performs two compact fields using the 320 ms Piano grouping, and returns to a visible `0 · 4 · 7 → 0 · 3` transition; browser-operation delay placed the final Sol outside the second compact field, so the model correctly ignored that later lone attack. The retained comparison reports one key step of motion, modeled friction `+12`, fusion `−5`, and openness `−15`, while fixed-spectrum assumptions, missing inherited membership, and unclaimed experience remain explicit. The generated Harmony instrument stays closed by default. At 1280 pixels the document remains 1280 pixels wide, the transformation and five-lens strip fit without overflow, and browser diagnostics contain no warnings or errors. At 390 pixels the document remains 390 pixels wide; the transformation, affordances, lenses, action, and disclosure stack into one readable column. These checks validate deterministic grouping, transposition boundaries, model provenance, state continuity, and responsive layout—not actual keyboard or DAW audio, a heard chord identity, tonal function, calibrated perception, listener response, emotion, preference, correctness, musicality, goodness, or learning gains.

The v1.55 Ear fixtures require an overlapping MIDI 60→67 pair to preserve seven equal-key steps, 700 cents, a 1.498:1 equal-tempered ratio, and its measured overlap while exposing four model readings. The sine assumption has no upper-partial alignment; harmonic and piano-proxy assumptions add declared partial interactions and may change fusion. Octave transposition preserves the relationship while allowing register-dependent modeled brightness to change. A sequential pair and a release-unknown pair retain interval facts but must emit no simultaneous auditory-model readings; invalid events and undersized phrases are refused. Browser review begins from the empty Ear bridge, performs Do held into Sol in Piano, and returns to two retained attacks with +7 steps, 1.498:1, 261.6→392.0 Hz under the A4=440 reference, and 284 ms proven overlap. The four assumptions report friction `12 · 6 · 6 · 6`, harmonic fit `100 · 100 · 100 · 99`, and fusion `70 · 77 · 75 · 77`; sine exposes `0 aligned · 0 near`, while the richer models declare their generated partial evidence. The five lenses keep measured MIDI, derived reference coordinates, modeled auditory evidence, and listener-only experience separate, and the generated Ear instrument stays closed by default. At 1280 pixels the document remains 1280 pixels wide with four invariant columns and five lens columns. At 390 pixels the document remains 390 pixels wide; invariants and lenses become one 352-pixel column, actions fit, and the disclosure stays closed. Browser diagnostics contain no warnings or errors. These checks validate deterministic interval arithmetic, release-based evidence gating, declared model sensitivity, state continuity, and responsive layout—not actual keyboard or DAW audio, calibrated pitch or loudness, heard fusion, consonance, listener response, emotion, preference, correctness, musicality, goodness, or learning gains.

The v1.56 Interval Context Echo fixtures require explicit source and echo pairs to preserve a seven-step 1.498:1 relationship while independently classifying overlap, separation, and missing release. A melodic source emits no simultaneous model readings; an overlapping transposed echo may emit four declared-model readings without backfilling the source. Backward chronology is rejected, while a restored phrase shifted onto a negative relative-time origin remains valid when event order and release-after-attack constraints hold. Browser review performs overlapping Do–Sol, freezes its seven-step span, then replays sequential Re–La. The five lenses retain `7 steps · 1.498:1`, report a +2-key center shift, change the hertz gap from 130.4 to 146.3, the attack gap from 303 to 550 ms, the release evidence from 273 ms overlap to sequential, and the sounding field from two notes to one. The Sound lens refuses roughness and partial-overlap comparison because only one pair overlapped. Reload testing exposed and fixed the negative restored-time edge case; a sequential restored Re–La source then compares safely with overlapping Mi–Ti without fabricating source spectra. The optional interval tools remain closed. At 1280 pixels the 1,210-pixel lesson fits the document and the lenses form three 369-pixel columns. At 390 pixels the document remains 390 pixels wide, the lesson is 352 pixels, each lens becomes one 316-pixel column, actions fit, and the disclosure stays closed. Browser diagnostics contain no warnings or errors. These checks validate interval transfer, temporal-role boundaries, restored-state arithmetic, provenance separation, and responsive rendering—not actual audio, calibrated hearing, harmonic function, listener similarity, emotion, correctness, musicality, preference, goodness, or learning gains.

The v1.57 Interval Experience Handoff browser path begins with a matched Do–Sol source and Re–La replay, then activates `Reflect on source + echo`. Experience receives the exact four ordered attacks while the live phrase still reports four attacks. The toolbar identifies `interval source + echo`, reports `4 captured attacks · comparison held`, and offers `Use whole live phrase` as an explicit exit from the bounded comparison. The first two prompts correctly refer to `this source-and-echo comparison`; all four listener answers remain separate from the measured and modeled evidence rows. Saving produces one browser-local observation, changes the matching-signature copy to `1 prior report shares`, and exposes `1 live phrase reflection` plus enabled JSON export in Personal Lens. A second clean tab repeats the full echo-to-reflection path under a 390-by-844 viewport: the document stays 390 pixels wide and the Experience toolbar and question each fit a 352-pixel column with no horizontal overflow. These checks validate exact-specimen continuity, context-aware copy, local persistence, Personal Lens handoff, evidence boundaries, and responsive flow—not actual audio, perceptual similarity, emotion inference, musicality, quality, or learning gains.

The v1.58 Chord Change Experience Handoff browser path performs Do–Mi–Sol and Fa–La–upper-Do as two learner-grouped gestures under the 320 ms setting. Chords focus renders one five-lens question. It reports `3→3 sounding · span 7→7 keys`, modeled roughness `−5`, entered Fa and La, left Mi and Sol, retained Do, one parallel nearest-key interpretation with largest leap 5, bass motion up 5, total travel 15, selected-Do pull `24→18`, repose `96→88`, and one fifths step. Each value stays in its own measured, interpreted, modeled, or listener-only lane. `Reflect on chord change` hands the exact six attacks to Experience as `chord before + after` and `6 captured attacks · change held` without shortening the six-attack live phrase. Leaving for Chords and returning through Experience initially exposed a continuity defect that silently replaced the bounded specimen with the whole phrase; the focus-selection guard now preserves any valid interval or chord specimen until `Use whole live phrase`, while Clear explicitly restores phrase mode. Four context-aware answers save through `Save this chord-change report`, increment local observations from one to two, and appear as `2 live phrase reflections` with enabled JSON export in Personal Lens. Under a 390-by-844 viewport the document stays 390 pixels wide, the chord panel is 352 pixels, and each five-lens article is 316 pixels with no horizontal overflow. Type checking, focused lint, rendered-source contracts, and all 165 deterministic tests pass. These checks validate corrected gesture selection, exact-specimen continuity, provenance boundaries, local persistence, focus navigation, reset safety, and responsive flow—not actual keyboard or DAW audio, intended voice leading or fingering, heard consonance, harmonic correctness, emotion inference, musicality, quality, or learning gains.

The v1.59 Chord Voicing Echo browser path selects the New voicing question, groups Do–Mi–Sol at 320 ms, freezes it, and performs Mi–Sol–upper-Do. The relationship matches as the same pitch-class set with a new bass role while span changes `7→8` keys, modeled roughness changes `−3`, bass-relative shape changes `0 · 4 · 7 → 0 · 3 · 8`, hand center rises four keys, nearest-key travel totals twelve, and selected-Do pull and home evidence remain `24→24` and `100→100`. A later Mi–Sol–Ti mismatch reports a changed relationship without discarding the source or offering reflection; replaying the inversion matches again. Reflection receives the exact source and latest matched attempt as `chord source + voicing` and `6 captured attacks · comparison held` while the complete phrase retains all nine attacks. Chords and Experience navigation plus reload preserve the bounded origin and frozen source. One note and Two chords modes hide the inactive lesson while retaining the source for return. At 390 by 844 pixels the document remains 390 pixels wide, the guide and echo region are 352 pixels, and each lens is 316 pixels. Type checking, focused lint, production export, source contracts, and all 168 deterministic tests pass. These checks validate pitch-class invariance, independent retries, state continuity, evidence provenance, reflection bounds, and responsive layout—not actual keyboard or DAW audio, intended fingering or voices, acoustic similarity, harmonic function, emotion inference, correctness, preference, musicality, goodness, or learning gains.

The v1.60 Chord Motion Echo browser path groups Do–Mi–Sol → held-Do–Fa–La under the 320 ms setting, then freezes those corrected fields from inside the existing Two chords lesson. Replaying Re–Fi–La → held-Re–Sol–Ti matches the entire move under one shared `+2`-key shift. Both source and replay report spans `7→9`, nearest-key travel `3`, largest leap `2`, and held bass, while selected-Do pull changes independently as `24→18` versus `44→78`. An explicit retry then performs Re–Fi–La → La–Di–Mi: both endpoint chord types return, but their shifts disagree at `+2` then `+4`, so the complete move is correctly labeled changed and no reflection action appears. A second retry reproduces the original move exactly and restores the match. Reflection receives `10 captured attacks · comparison held` as `chord move source + replay`, while the complete live phrase retains twenty attacks. Chords navigation and reload preserve the source, explicit attempt boundary, and match. At 390 by 844 pixels the document remains 390 pixels wide; the echo region is 352 pixels, every lens and both actions are 316 pixels, and no runtime error surface appears. Type checking, focused lint, source contracts, and all 169 deterministic tests pass. These checks validate shared-shift invariance, endpoint-mismatch diagnosis, explicit retries, inherited-event deduplication, state continuity, reflection bounds, and responsive layout—not actual keyboard or DAW audio, intended fingering or voices, acoustic identity, harmonic function, expectation, tension, resolution, emotion, correctness, preference, musicality, goodness, or learning gains.

The v1.61 Tonal Frame Echo browser path reuses the source Do–Mi–Sol → held-Do–Fa–La and matches it as Fa–La–lower-Do → held-Fa–Li–Re under one shared `+5`-key shift. With Do fixed, selected-context pull reads `24→18` for the source and `18→53` for the replay; home evidence reads `100→80` versus `80→17`. Choosing `Move Do +5` changes only the replay reference frame and restores both paths exactly to `24→18` and `100→80`. Sound, Relationships, and Motion lens text remains byte-for-byte unchanged, including shared-shift identity, travel `3 vs 3`, and largest leap `2 vs 2`; the visible microscope remains seven attacks. The pressed state moves between the two native controls, replay SVG titles adopt the moved relative roles, and the Experience lane still requires the listener's report. At 390 by 900 pixels the document and scroll width remain 390 pixels, the echo is 352 pixels, and the question and two-button group are 316 pixels with no overflow. Type checking, focused lint, production export, source contracts, and all 170 deterministic tests pass. These checks validate reference-frame invariance, evidence separation, non-mutating presentation state, accessibility, and responsive layout—not actual keyboard or DAW audio, intended fingering or voices, acoustic identity, heard tonal function, expectation, tension, resolution, emotion, correctness, preference, musicality, goodness, or learning gains.

The v1.62 Octave Fold browser path freezes Do–Mi–Sol, then matches Mi–Sol–upper-Do as a new inversion with a changed bass role. The physical view reports `3→3` voices, span `7→8` keys, bass-relative shapes `0 · 4 · 7 → 0 · 3 · 8`, nearest-key travel `12`, modeled roughness `−3`, selected-Do pull `24→24`, and home evidence `100→100`. Activating `Fold into one octave` replaces that register axis with aligned closed gap loops `3 · 5 · 4 = 3 · 5 · 4`; the pressed state becomes true, the five lens strings stay byte-for-byte unchanged, and the five performed attacks remain intact. The model fixture additionally folds a four-key octave-doubled field to three unique positions and reports one removed doubling; inversion and +2-key transposition preserve the canonical loop, while changing the third does not. A later performed mismatch restores the physical view and removes the progressive fold question. At 390 by 900 pixels the document and scroll width remain 390 pixels; the echo is 352 pixels, and the question, button, folded figure, and reading are each 316 pixels. Browser diagnostics contain no warnings or errors. Type checking, focused lint, production export, source contracts, and all 171 deterministic tests pass. These checks validate the equal-tempered octave-fold invariant, progressive state boundaries, evidence non-mutation, accessibility, and responsive layout—not actual keyboard or DAW audio, tuning-independent chord identity, intended fingering or voices, acoustic or perceptual identity, root, tonal function, consonance, emotion, correctness, preference, musicality, goodness, or learning gains.

The v1.63 Gap Mutation browser path freezes Do–Mi–Sol and performs Do–Ri–Sol as the latest independent attempt. Before reveal, the five lenses report `3→3` voices, span `7→7`, modeled roughness `+1`, bass-relative shapes `0 · 4 · 7 → 0 · 3 · 7`, oblique nearest-key motion with largest leap and total travel `1`, selected-Do pull `24→25`, and home evidence `100→92`. The progressive question appears only because two octave positions remain and exactly one changes. Activating `Compare changed gaps` replaces the physical coordinate with source `4 · 3 · 5` and replay `3 · 4 · 5`, visible replay labels `3 Δ−1` and `4 Δ+1`, two changed adjacent gaps, retained-Do comparison alignment, and explicit twelve-step closure. The five lens articles remain present and browser diagnostics contain no warnings or errors. A later structural match receives the original Octave Fold question rather than Gap Mutation; a C–Re–Fi mismatch with only one retained source position removes the gap question entirely and restores the physical figure. At 390 by 844 pixels the document remains 390 pixels wide and the question, action, figure, reading, and five-lens region are each 316 pixels. The deterministic model fixture checks the same arrays, deltas, signed displacement, retained positions, closure sums, invalid inputs, and refusal boundaries. Type checking, focused lint, production export, source contracts, and all 172 deterministic tests pass. These checks validate equal-tempered set-position comparison, progressive state boundaries, explicit deltas, evidence non-mutation, accessibility, and responsive layout—not intended voice identity, actual keyboard or DAW audio, acoustic causality, heard chord identity, root, function, emotion, correctness, preference, musicality, goodness, or learning gains.

The v1.64 Scale Landing Mutation browser path first authors `2–2–1–2–2–2–1`, then performs `2–1–2–2–2–2–1` from the same on-screen register with the third internal landing lowered by one equal key. The comparison reports source positions `0, 2, 4, 5, 7, 9, 11, 12`, new positions `0, 2, 3, 5, 7, 9, 11, 12`, `Landing 4 → 3`, seven retained positions including both closure edges, two changed neighboring gaps, and visible `Δ−1`/`Δ+1` labels while both routes still total twelve. Restarting the experiment creates no silent target; reproducing the source gives the explicit “Every landing stayed fixed” control refusal. Model fixtures separately cover changed landing count, multiple moved landings, invalid nonclosing arrays, and unchanged inputs. At 390 by 844 pixels the document stays 390 pixels wide; the 288-pixel mutation region and route rails do not overflow, and the gap comparison reflows to four 71.25-pixel columns. Browser diagnostics contain no warnings or errors. Type checking, focused lint, source contracts, all 171 deterministic model/accessibility tests, and both rendered-page tests pass. These checks validate deterministic equal-tempered gap redistribution, progressive exercise state, refusal boundaries, non-color labels, and responsive layout—not acoustic output, intonation outside twelve-equal temperament, note or scale names, tonal function, emotion, preference, goodness, or learning gains. Physical MIDI-device and DAW-throughput checks remain external.

The v1.65 Interval Ripple browser path reuses that exact `4 → 3` landing change and activates the native `Every connected interval` view. It displays seven source/new spokes: distances from the moved landing to retained positions `0, 2, 5, 7, 9, 11, 12` change from `4, 2, 1, 3, 5, 7, 8` to `3, 1, 2, 4, 6, 8, 9`. Every row states shorter or wider by one key step and exposes the exact 12-TET multiplier rounded to three decimals; the first row is `4 · ×1.260 → 3 · ×1.189`. The reading reports seven changed spokes and twenty-one invariant retained-to-retained relationships while warning that absolute frequencies may differ under transposition. A second performed route with positions `0, 2, 5, 7, 8, 9, 11, 12` moves one landing by four keys, receives the minimal-change refusal, and exposes no ripple toggle. At 390 by 844 pixels the document, 288-pixel comparison, spoke field, and stacked two-choice control remain overflow-free. Browser diagnostics contain no warnings or errors. Type checking, focused lint, production export, source contracts, all 172 deterministic tests, and both rendered-page tests pass. These checks validate normalized equal-key distance propagation and invariant counts—not absolute frequency identity, tuning beyond 12-TET, acoustic output, consonance, function, emotion, preference, goodness, or learning gain.

The v1.66 Spectral Ripple browser path continues the same successful `4 → 3` intervention, opens `Every connected interval`, and selects the changed spoke to retained position `0`. The comparison reuses the original 261.6 Hz route origin for both counterfactuals: the source four-step pair is 261.6 → 329.6 Hz and the new three-step pair is 261.6 → 311.1 Hz even if the performed mutation began elsewhere. Under the nine-partial harmonic model, both rows expose one 18-cent alignment and six displayed non-aligned interaction zones while the separate roughness proxy changes 19 → 28 and overlap changes 6 → 5; the interface therefore does not manufacture one aggregate direction. Switching the declared assumption to sine redraws exactly one component per tone and updates the same source/new coordinate without changing MIDI or interval facts. The first browser pass exposed and then fixed a real rendering crash caused by passing `frequencyFromMidi` directly to `Array.map`, which supplied the array index as an invalid tuning argument; the repaired path uses an explicit one-argument callback. After repair, the full source/new strips, accessible summaries, native spoke selection, restored-session source register, model switch, and live delta reading render successfully. At 390 by 844 pixels the document, selected spoke, spectrum field, both stacked strips, SVGs, and reading remain 390 pixels or narrower; the main spectrum region is 288 pixels wide with no horizontal overflow. No new warning or error was logged after the repaired build. Type checking, focused lint with only two pre-existing unused-destructure warnings, production export, source contracts, all 173 deterministic tests, and both rendered-page tests pass. These checks validate declared-model counterfactual consistency—not actual keyboard or DAW audio, consonance, tonal function, emotion, preference, goodness, or learning gain.

The v1.67 Ending Ripple browser path performs phrase A as Do–Mi–Sol–Fa and declares `Change the ending`, then performs phrase B as Do–Mi–Sol–Sol with matched timing and releases. The existing five-lens gate reports one of three signed moves changed, the proportional timing path and overlap invariant, ending role Fa → Sol, and a full gravity-model candidate change from Fa to Sol while leaving the listener’s three reports unanswered. Only after that controlled result does the native `Trace every ending relationship` reveal appear. It normalizes both phrases to positions `0, 4, 7`, expands the ending `+5 → +7` into three source/new signed spokes—`+5 → +7`, `+1 → +3`, and `−2 → 0`—shows frequency multipliers, identifies the final approach `−2 → 0`, and counts three invariant earlier-to-earlier relationships. Deterministic fixtures also verify a uniformly transposed replay, unchanged-ending refusal, earlier-position-change refusal, unequal-count refusal, invalid MIDI rejection, exact ratios, and invariant combinatorics. At 390 by 844 pixels the document remains 390 pixels wide; the 332-pixel ripple and 312-pixel spokes/readout remain overflow-free, the reveal retains a native pressed state, and source/new marks remain distinguishable by solid versus dashed form. Browser diagnostics contain no warnings or errors. Type checking, focused lint, production export, source contracts, all 174 deterministic tests, and both rendered-page tests pass. These checks validate normalized one-ending relational propagation and progressive disclosure—not cadence identity, actual audio, acoustic causation, tonal-function certainty, emotion, preference, correctness, musical quality, or learning gain.

The v1.68 Chord Gesture Echo fixture holds a two-field pitch-class transformation constant while changing its performed timing: the source spreads its attacks `40 → 30 ms`, places the second field `600 ms` after the first anchor, and proves `50 ms` of pedal-ended overlap; the replay spreads `80 → 10 ms`, places the second field after `400 ms`, and proves `50 ms` of silence. Mean MIDI attack values change independently. Additional cases keep an unresolved pedal release unknown, refuse missing fields or reversed chronology, and reject invalid velocity, release ordering, or non-finite time data. Source contracts require a native expanded-state reveal, a named timeline SVG, exact source attack IDs rather than inherited chord members, non-color circle/square field marks, dashed pedal tails, forced-color mappings, and a one-column narrow layout. Type checking, focused lint, the GitHub Pages production export, all 175 deterministic tests, and both rendered-page tests pass; the built page carries the v1.68 marker. Live pointer interaction, 390-pixel geometry inspection, physical MIDI release/pedal behavior, and DAW/browser coexistence remain external checks for this milestone. These automated checks validate measured MIDI gesture separation—not meter, groove, intention, acoustic loudness, emotion, preference, correctness, musical quality, or learning gain.

The v1.69 Resolution Landing Lens fixture freezes a fork source, finds the first later attack in the selected destination pitch class, and keeps the original source-to-destination intention separate from the actual final approach. Direct and detoured cases pin signed equal-key motion, 12-TET frequency ratio, source and final onset gaps, intervening attack count, attack-velocity change, and release evidence as silence or pedal-ended overlap; malformed notes, velocities, chronology, releases, and target pitch classes are rejected. The exact source event is persisted with the fork and restored into the evidence path after the rolling sixty-second phrase has pruned it. Source contracts require a native expanded-state reveal, a named pitch-over-time figure, five separate Sound/Relationships/Motion/Context/Experience lenses, a bounded Personal Character handoff, explicit zero-remaining-pull explanation, one-column narrow actions, forced-color landing marks, and no resolution, function, causation, style, or goodness claim. Live browser review performs a silent Do–Mi–Sol context, arms Return to the center, lands on Do, opens the lens, and confirms the frozen Sol→Do path, reciprocal fifth relationship, final release bridge, independent selected-frame pull/home evidence, enabled four-attack reflection handoff, and no browser errors. Type checking, focused lint, the GitHub Pages production export, all 176 deterministic tests, and both rendered-page tests pass. Exact 390-pixel geometry, physical MIDI behavior, and DAW/browser coexistence remain external checks for this milestone. These checks validate intended-destination continuity and evidence separation—not cadence identity, actual audio, acoustic causation, heard resolution, harmonic function, emotion, preference, correctness, musical quality, or learning gain.

The v1.70 Phrase Breath Map fixture uses six released attacks with one longer quiet gap to pin a 200-millisecond local onset-group reference, a 360-millisecond candidate threshold, a 120-millisecond minimum proven silence, two timing islands, exact event membership, pitch spans, and signed interval paths. A stricter three-times threshold merges the same fixed phrase into one island; unresolved release evidence and pedal-ended overlap refuse the boundary. A separate compact-chord fixture places three attacks inside one bounded onset group before computing the phrase ruler, proving that a played chord does not masquerade as several beats and that the latest release across the whole group governs quiet-space evidence. Duplicate IDs, invalid thresholds, and malformed release timing are rejected. Live browser review plays five silent on-screen attacks with one deliberate pause and confirms five onset groups, one release-proven candidate gate, two timing islands, exact attack labels, pitch paths, interval summaries, provenance copy, and no browser warnings or errors. At 390 by 844 pixels the document remains 390 pixels wide; the Motion questions reflow to two columns with the fifth spanning the row, the 352-pixel Breath region does not overflow, and timing-island summaries stack to one column. Type checking, focused lint, the GitHub Pages production export, all 177 deterministic tests, and both rendered-page tests pass. Physical MIDI release and pedal behavior, DAW/browser coexistence, listener interpretation, and learning effectiveness remain external checks. These checks validate transparent timing-group hypotheses and conservative release gating—not intended phrasing, breath, meter, form, acoustic sound, expression, emotion, preference, correctness, musical quality, or learning gain.

The v1.71 One-Pause Counterfactual fixture keeps a five-attack absolute pitch path fixed while changing only the third of four onset gaps by 400 milliseconds. It pins the one-gap result, three retained control gaps, the source and replay silence values, and the exact changed gap index; sub-45-millisecond drift remains inside tolerance, uniform 1.5-times retiming produces a multiple-gap refusal, changed keys produce a pitch-control refusal, unresolved release reason stays unknown even when a frozen specimen has a bounding timestamp, and reversed chronology is rejected. Live browser review performs Do–Re–Mi–Sol twice through the existing learner-declared timing experiment, lengthens only the second gap, and confirms the control-preserved five-lens result before progressive disclosure offers the microscope. The revealed shared axis reports `734 → 1633 ms`, an observed `+898 ms` change, two retained gaps, and release-proven silence `386 → 1284 ms`; the experience lane remains unanswered. At 390 by 844 pixels the document remains 390 pixels wide and the 332-pixel invitation and detail regions do not overflow. Browser diagnostics contain no warnings or errors. Type checking, focused lint, the GitHub Pages production export, all 178 deterministic tests, and both rendered-page tests pass. Physical MIDI timing, DAW/browser coexistence, perceptual timing thresholds, listener reports, and learning effectiveness remain external checks. These checks validate one performed timing intervention and evidence separation—not a phrase boundary, meter, expressive intention, emotion, preference, correctness, musical quality, or causal effect.

The v1.72 Motif Invariance Lens fixtures pin a three-attack shape replayed seven keys higher with identical signed interval path and normalized timing shares, and a four-attack same-pitch return whose third proportional gap changes most. They verify zero-anchored relative pitch positions, signed adjacent moves, statement-local gap shares, preserved pitch shape, changed timing classification, start shift, and contradiction guards for event identity and chronology. Source contracts require a native progressive reveal, a named nonvisual summary, a three-row aligned comparison, an explicit normalization explanation, narrow-width reflow, forced-color controls, and refusal of intended-motif, formal-function, emotion, correctness, and quality claims. Live browser review locks movable Do, performs Do–Re–Mi followed by Sol–La–Ti, confirms the `+7` transposed-shape match, and reveals identical `0 → +2 → +4` relative pitch paths, identical `+2 · +2` signed moves, timing shares of `51% · 49%` versus `48% · 52%`, and the separated start-shift change. At 390 by 844 pixels the document, body, 316-pixel Motif field, 288-pixel reveal, and 266-pixel grid remain inside the viewport with no horizontal overflow. Browser diagnostics contain no warnings or errors. Type checking, focused lint, the GitHub Pages production export, all 180 deterministic tests, and both rendered-page tests pass. Physical MIDI timing, learner comprehension, perceptual thresholds, and learning effectiveness remain external checks.

The v1.73 Learner-Bounded Motif Echo fixture compares a declared four-attack source against a full transposed return, a same-opening altered ending, and a multi-property change. It pins shared signed moves, a seven-key start shift, the final changed move, ending delta, whole-window classification, count refusal, identity guards, and chronology rejection. Source contracts require native three- and four-attack freeze actions, explicit replay progress, full-boundary language, retry and release controls, a named status region, session-version persistence, narrow one-column actions, forced-color controls, and reuse of the nonvisual invariance summary. Live browser review locks Do, performs Do–Re–Mi–Fa, freezes all four attacks, and replays Sol–La–Ti–Do. The result retains the complete `+2 · +2 · +1` move path, reports a `+7` start shift, displays `35% · 34% · 31%` versus `33% · 34% · 33%` proportional gaps, and states that every chosen attack entered the comparison. Reload restores the exact source and result at the URL-addressed Motif mode; retry preserves the source and returns to `0/4`, while release restores passive source selection. At 390 by 844 pixels the 316-pixel Motif field, 288-pixel exercise, 288-pixel progress region, and 288-pixel action region remain inside a 390-pixel document with no horizontal overflow. Browser diagnostics contain no warnings or errors. Type checking, focused lint, the GitHub Pages production export, all 181 deterministic tests, and both rendered-page tests pass. Learner comprehension, physical MIDI behavior, perceptual thresholds, and learning effectiveness remain external checks.

The v1.74 Motif Return Arc fixture pins the chronological state machine for an exact return, an altered-ending variation, and a later transposed relationship return. Source contracts require the named source → change → return summary, live open/closed status, non-color dashed-versus-double line styles, forced-color distinctions, one-column narrow reflow, backward-compatible v1.73 migration, and session version 21 persistence. Live browser review performs Do–Re–Mi–Fa, freezes all four attacks, changes only the ending to Sol, keeps that variation, and then returns to Do–Re–Mi–Fa. The HUD first says that the variation remains open and then shows source → ending changed → exact relationship with “The relationship returned after a variation.” Reload restores the source, both statements, and the completed arc. At 390 by 844 pixels the document remains exactly 390 pixels wide, the return trail is 288 pixels wide with one 262-pixel column, and no horizontal overflow appears. Type checking, focused lint, deterministic tests, rendered-page tests, and the GitHub Pages production export are release gates; physical MIDI behavior, assistive-technology review, learner comprehension, and learning effectiveness remain external checks.

The v1.75 Motif Experience Check extends the state fixture so a newer variation after a completed return reopens the arc instead of inheriting a stale completed state. Source contracts require a named experience region, a labeled modeled-versus-reported comparison, one native pressed-state group, live report status, forced-color selection, one-column narrow reflow, migration with no report, and session version 22 persistence. Live browser review restores the v1.74 source → altered ending → exact return specimen, chooses “Yes, a return,” and confirms both the pressed state and “You heard the last statement as a return” survive reload. Keeping that return and playing a new altered ending hides the experience question, restores “The variation is open,” and exposes the correct “Keep variation + try a return” action. Keeping the variation and replaying the exact source closes the new arc with a fresh unreported question. At 390 by 844 pixels the document remains exactly 390 pixels wide; the 288-pixel experience region, evidence pair, and report choices each collapse to one 288-pixel column without overflow. Type checking, focused lint, deterministic tests, rendered-page tests, and the production export remain release gates; physical MIDI behavior, assistive-technology review, learner comprehension, listener validity, and learning effectiveness remain external checks.

The v1.76 Return Experience Contrast fixture pins event-addressed insertion, in-place report replacement, chronological retention, four-observation bounding, and refusal of non-return comparisons. Source contracts require particular-performance/no-average language, a named nonvisual contrast, dashed transposed-return encoding, current-return marking, one-column narrow reflow, forced-color distinctions, v1.75 compatibility, and session version 23. Live browser review migrates the existing exact return by choosing “Yes, a return,” keeps a new altered-ending variation, then performs Sol–La–Ti–Do as a +7-key transposed return and reports “Not sure.” Only then does the contrast appear with same starting key beside the first report and start shifted +7 beside the second. Changing the second report to “Not as a return” leaves exactly two observations and replaces the current text; reload retains both. At 390 by 844 pixels the document remains exactly 390 pixels wide, while the 288-pixel contrast collapses to one 288-pixel column without overflow. Type checking, focused lint, deterministic tests, rendered-page tests, and the production export remain release gates; physical MIDI behavior, assistive-technology review, learner comprehension, listener validity, and learning effectiveness remain external checks.

The v1.77 Motif Experience Handoff fixture pins selection of the latest completed source → variation → relationship-return cycle and rejects incomplete, unequal-length, duplicate-ID, and expired-event specimens. Source contracts require an explicit whole-arc action, exact-event and aged-out language, motif-specific one-question prompts, arc-specific held/specimen/save labels, and the same measured/modeled/reported separation used throughout the Personal Character Map. Browser review follows the handoff from a completed four-attack motif cycle, confirms that all twelve original attacks enter the frozen reflection, answers the four listener questions, and saves one local observation without changing the retained live phrase. Narrow review at 390 by 844 pixels keeps the handoff and Personal Character workflow inside the document with no horizontal overflow. Type checking, focused lint, deterministic tests, rendered-page tests, and the production export remain release gates; physical MIDI behavior, assistive-technology review, learner comprehension, listener validity, and learning effectiveness remain external checks.

The v1.78 Landmark Experience Handoff fixture pins ordered field-to-event flattening, first-occurrence preservation for sustained attacks shared between fields, exact field-count validation, per-field identity validation, and the minimum complete specimen boundary. Personal-response parsing accepts only bounded landmark labels with an original, transposed, or one-key-changed route and remains backward compatible with observations that have no context. Browser review performs all four fields of the generated pop loop, confirms an exact twelve-attack reflection, answers and saves all four listener questions, verifies the route label in Personal Lens, and reloads the completed path with the reflection still available. At 390 by 844 pixels the Paths, Experience, and Personal surfaces remain exactly 390 pixels wide with no horizontal overflow; browser diagnostics contain no warnings or errors. Type checking, focused lint, all deterministic tests, rendered-page tests, and the GitHub Pages production export remain release gates; physical MIDI behavior, acoustic sound, assistive-technology review, learner comprehension, listener validity, stylistic generalization, and learning effectiveness remain external checks.

The v1.79 Landmark Experience Contrast fixture pins latest-eligible-pair selection, nearest-earlier same-path pairing, chronological A/B retention, +7 equal-key center movement, one-key-mutation distinction, one-report refusal, optional bounded path labels and center positions, and invalid-center rejection. Source contracts require a named region, separate four-dimension listener plot, circle-versus-diamond non-color marks, measured-versus-modeled evidence labeling, narrow one-column reflow, forced-color distinctions, and explicit no-causation/no-goodness language. Browser review reuses one v1.78 pop-loop report, performs and saves a new twelve-attack route, and confirms `75 → 25` settledness, `75 → 50` energy, `75 → 75` familiarity, and `75 → 100` liking beside measured key span and overlap and modeled crunch and ending repose. The older report's absent center produces an honest unavailable statement; reload preserves the comparison. At 390 by 844 pixels the document remains exactly 390 pixels wide, the contrast is 352 pixels wide, both performance labels and all four evidence cells collapse to one column, and browser diagnostics contain no warnings or errors. Type checking, focused lint, all 186 deterministic tests, both rendered-page tests, and the GitHub Pages production export remain release gates; physical MIDI behavior, acoustic sound, assistive-technology review, causal inference, preference stability, learner comprehension, listener validity, stylistic generalization, and learning effectiveness remain external checks.

The v1.80 Landmark Route Fingerprint fixture pins the pop loop's four folded field sets `0·4·7 → 2·7·11 → 0·4·9 → 0·5·9`, authored root offsets `0 → 7 → 9 → 5`, carried-position sets `7`, none, and `0·9`, fifths travel `1 → 2 → 4`, bounded nonnegative nearest-key motion, the single declared `0 → 1` shadow-field mutation, and empty-path refusal. Source contracts require a named plot, complete nonvisual route summary, separate root/tone/change shapes, exact-held line encoding, transition list, completion-only disclosure, narrow one-column transition reflow, forced-color distinctions, and explicit transposition and evidence limits. Browser review performs all four pop-loop fields and confirms the exact accessible fingerprint, three transition summaries, and separation from register, timing, assumed sound, tonal interpretation, and experience. At 390 by 844 pixels the document remains exactly 390 pixels wide, while both the fingerprint and its plot remain 316 pixels wide and transition summaries collapse to one column; browser diagnostics contain no warnings or errors. Type checking, focused lint, all 187 deterministic tests, both rendered-page tests, and the GitHub Pages production export remain release gates; physical MIDI behavior, acoustic sound, assistive-technology review, learner comprehension, stylistic validity, listener interpretation, and learning effectiveness remain external checks.

The v1.81 Performed Route Comparison fixture pins pop as four unique fields with carried-count path `1·0·2`, blues as five fields containing two repeated fields and an explicit return to its opening field, pedal point as one repeated field with position `0` present through every field, the two exact field shapes shared by pop and pedal, and the three exact shapes retained when only pop's declared counterfactual field changes. Comparison equality checks remain sequence-specific for field order, carried tones, nearest-key motion, and fifths travel; no aggregate score exists. Source contracts require two physical route completions, a named nonvisual A/B plot, non-color dashed shared-shape capsules, a semantic property table, local version-25 session validation, a narrow two-column table reflow, forced-color marks, and explicit refusals of song matching, style detection, emotional prediction, and goodness. Browser review completes the pop loop, holds it as A, completes the pedal-point route as B, verifies two highlighted exact field shapes and the through-tone contrast, then switches B to the classical cadence without losing A. Desktop and 390-pixel checks require no horizontal overflow or browser diagnostics. Type checking, focused lint, all deterministic tests, rendered-page tests, and the GitHub Pages production export remain release gates; physical MIDI hardware, acoustic sound, assistive technology, learner comprehension, stylistic validity, listener interpretation, and learning effectiveness remain external checks.

The v1.82 Landmark Transition Change Lens fixture pins the pop loop's first move as folded `stayed 7`, `entered 2·11`, and `left 0·4`; stable reference targets `60·64·67 → 59·62·67`; total nearest-key motion `3`; largest leap `2`; and root travel `1` fifths step. A separate counterfactual fixture requires the changed field's declared `0 → 1` mutation to survive selection, while invalid edge indices and non-finite centers return no lesson. Source contracts require native pressed-state transition selection, a named nonvisual physical-key plot, five separately labeled evidence lenses, an explicitly unclaimed Experience answer, one-column narrow reflow, forced-color distinctions for held/added/released motion and before/after marks, and refusal of reconstructed sound, timing, pedal, intention, emotion, or goodness claims. Browser review completes the pop route, opens move `1 → 2`, verifies membership changes and five-lens deltas, changes the assumed spectrum while structural and motion evidence remain fixed, closes the lesson, and confirms no horizontal overflow or diagnostics at desktop and 390 pixels. Type checking, full lint, all deterministic tests, rendered-page tests, and the GitHub Pages production export remain release gates; physical MIDI hardware, acoustic sound, assistive technology, learner comprehension, listener interpretation, and learning effectiveness remain external checks.

The v1.83 Shared Cycle Lens fixture pins the equal-tempered bright triad to the compact `4:5:6` candidate, a transposed copy to the same harmonic integers and cents mismatch with a proportionally shifted base, the shadow triad to a visible `6:7:9` versus `10:12:15` complexity/fit tradeoff, and a one-key pair to three downsampled Pareto candidates. Empty, duplicate-only, over-six-position, invalid MIDI, invalid harmonic-limit, and zero-result cases are refused. Source contracts require a progressive native reveal, candidate pressed states, a complete nonvisual waveform summary, explicit phase-reset disclosure, one-column narrow reflow, forced-color waveform and closure marks, and refusal of inferred root, chord identity, function, consonance, emotion, or goodness. Type checking, full lint, deterministic tests, rendered-page tests, a production export, and desktop/mobile browser review remain release gates; physical MIDI hardware, acoustic phase and spectrum, assistive technology, learner comprehension, listener interpretation, and learning effectiveness remain external checks.

The v1.84 MIDI Reference-Frequency Provenance contracts require every live Piano and Ear hertz reading to disclose its A4=440 12-TET derivation from MIDI key number, while key/timing facts remain separately labeled as measured. The primary plot, shared-cycle summary, partial microscope, interval echo, resolution landing, selected-attack, chord comparisons, sound-model disclosure, on-screen key names, and Ear invariant/lens copy must not call those reference coordinates measured acoustic fundamentals. Visible and screen-reader text also state that pitch bend, instrument or DAW tuning, acoustic pitch, and audio spectra are not captured. Documentation must describe the bounded sixty-second phrase and latest seven-attack microscope without conflating them. Type checking, full lint, all 191 deterministic tests, both rendered-page tests, the GitHub Pages production export, and a desktop browser walk through Piano, Shared Cycle, and the retained Ear bridge pass; browser diagnostics contain no warnings or errors. These checks validate provenance and copy consistency—not the tuning or spectrum of attached hardware, user comprehension, acoustic accuracy, musical interpretation, or learning effectiveness.

The v1.85 Resonance Sky fixture requires fixed note geometry across Do changes, equal unclamped octave-radius increments, deterministic trail output, bounded twenty-eight-event memory, register-spanning eight-note sampling, correct total/analyzed/omitted pair counts, evenly sampled twelve-link output without cross-landmark quality ranking, retained-phrase newness, matching-field crunch, all-retained-note silence proof, safe empty/repeated/extreme input, valid one/two/multi-point paths, and an Immersion lens that survives tab-summary parsing. Source contracts require a named region and SVG description, delayed atomic polite status, native assumed-spectrum select and optional silent hand-horizon disclosure, non-color fill/dash/double-ring/text redundancies, explicit A4=440 and audio-capture limits, 900- and 620-pixel reflow, a 390-pixel no-overflow immersive crop, reduced-motion final geometry, forced-color line art, and refusal of aggregate goodness or emotion claims. Desktop and 390-pixel browser checks cover empty and three-note states, bounded filaments, active stars, compact mobile controls, and no document overflow; browser diagnostics must remain clear. These checks validate deterministic geometry and interface contracts—not physical MIDI hardware, sustain/disconnect behavior, acoustic sound, assistive-technology announcements, comprehension, aesthetic response, or learning effectiveness.

The v1.86 Resonance Sky fixtures require a deterministic twelve-sector fifths crown with correct route-degree indices; three labeled attack-density tides whose circular mean crosses the `11 → 0` fifths seam correctly and whose center disappears for evenly opposed evidence; latest-attack role, signed motion, interval landmark, onset gap, A4=440 reference frequency, pitch-class occurrence count, and intervening-attack evidence; a newest-twelve-event contour that groups close attacks and connects only singleton onset groups as a melodic line; valid closed one-, two-, and multi-position chord hulls; separate bass-relative, cyclic, physical-gap, span, and doubling descriptions; and priority-ordered, collision-avoiding proximal annotations capped at five. Integration contracts require phrase-scoped chord grouping before the four-membrane display cap, an inner interpreted versus outer audible hull, exact/incomplete and together/rolled non-color boundaries, explicit live-field versus latest-attack-snapshot text, all-twelve selected and outside-route marks, honest leading/runner catalog language, contextual-center cues that remain separate from pitch-set fit, one detector-only relationship-window bridge, and at most two delayed in-route possibilities labeled as non-predictions. Source, rendered-page, desktop, 390-pixel, reduced-motion, forced-color, no-overflow, and clear-diagnostics checks remain release gates. They do not validate a heard key, chord function, intended melody or motif, next-note expectation, aesthetic response, comprehension, physical MIDI behavior, acoustic sound, or learning effectiveness.

The v1.87 hardening fixtures require a full 128-key chord field to collapse to a bounded hull path while retaining its source count, five recent grouped gestures to precede the four-membrane display cap, no SVG blur allocation, explicit dense-field crunch refusal above eight positions, and a clock that sleeps after the idle nearby-field reveal. Browser stress review completed sixty-four note attacks followed by ten simultaneously held distinct positions with bounded gradients, no filter nodes, an explicit paused-model notice, and no console warning or error. This is a targeted stability check rather than proof against every browser, GPU, driver, hardware-MIDI, or exceptionally long-session failure.

The v1.88 Semitone Horizon fixtures sanitize, deduplicate, and sort MIDI positions; preserve exact adjacent and compound distances; bin octave-equivalent pair spans without discarding their realized register distances; derive deterministic signed moves to selected Do; and keep the latest Immersion attack plus its nearest visible relationship under dense sampling. Sound-model fixtures confirm that the same seven-semitone span can produce different modeled roughness in different registers, so semitone count is never substituted for an acoustic or perceptual verdict. Source and accessibility contracts require exact semitone units across Scale, Harmony, interval, chord, voice-leading, and landmark views; a twelve-position non-color-redundant Immersion horizon; a major/minor/diminished atlas with a shared logarithmic frequency axis, A4=440 fundamentals, bounded declared partials, and corrected one-semitone down/hold/up routes; a bounded live-held crunch fallback; and explicit separation of spacing, assumed spectrum, selected-Do heuristic, and listener experience. Browser review at 390 by 844 pixels confirms a 358-pixel Immersion stage, twelve horizon positions, 352-pixel chord atlas, 314-pixel cards, 290-pixel frequency rows, Scale and Harmony reflow, and no horizontal overflow. A silent live Do–Mi–Sol field identifies the current major type, reports adjacent `4–3` and pair spans `3·4·7`, and restores modeled crunch rather than showing it unavailable. Desktop chord review keeps the three reference cards aligned. Browser warning/error logs are empty. Final gates pass full lint, type checking, all 220 automated tests including both rendered-page checks, and the GitHub Pages production export. The bundler still reports its advisory for a client chunk above 500 kB, so later route-level code splitting remains a network-payload optimization. These checks do not validate physical MIDI hardware or DAW routing, acoustic frequencies or spectra, required harmonic resolution, universal chord emotion, assistive-technology announcements, learner comprehension, or learning effectiveness.

The v1.89 Scale-Degree Harmonic Field fixtures pin all seven major-route stacks (`0–4–7`, `0–3–7`, and `0–3–6`), all five minor-pentatonic stacks (`0–5–10`, `0–4–9`, and `0–5–9`), and the whole-tone route's repeated symmetric `0–4–8`. They verify octave-unfolded degree numbers, exact local incoming/outgoing gaps, Do-minor first inversion above Me, Me-major second inversion above Te, whole-tone root ambiguity, and refusal of non-integer or non-twelve-semitone routes. Source contracts require a named section, native pressed-state scale and degree choices, one atomic nonvisual update, a named bass-relative ruler, forced-color marks, three-column 320-pixel reflow, and explicit separation of structural character from emotion, goodness, or required resolution. Production-browser review confirms the v1.89 label, pentatonic fourth-stack reading, complete text alternative, 390- and 320-pixel layouts with no horizontal overflow, a bounded audio-start failure instead of an indefinite preparing state, and no warning or error logs. Final gates pass full lint, type checking, 223 deterministic tests plus both rendered-page checks (225 total), and the GitHub Pages production export. The client-chunk advisory above 500 kB remains a later code-splitting optimization. These checks do not validate physical audio output, acoustic spectra, assistive-technology announcements, learner comprehension, listener interpretation, or learning effectiveness.

The v1.90 Scale Generator Laboratory fixtures pin the full `+7` pitch-class orbit, negative-generator normalization, `12 / gcd(12, g)` loop length and component count for every `+1` through `+7` move, the major-producing seven-stop fifth window, six retained positions under a one-fifth slide, and the exact old-Fa-to-Fi boundary replacement. They separately verify that rehoming unchanged major pitches on old Sol yields Mixolydian gaps; that five-stop, full-orbit, whole-tone, diminished-seventh, augmented-triad, tritone, and chromatic windows expose their actual closure and repetition; that diatonic, natural-minor, major-pentatonic, harmonic-minor, whole-tone, octatonic, and chromatic fifth shifts retain different memberships; and that the pure `3:2` spiral differs from 12-TET closure by the Pythagorean comma. Source contracts require native pressed-state stages and generator choices, a native window slider, explicit axis re-zeroing, complete atomic live summaries, named orbit and gap visuals, row headers that remain in the accessibility tree at narrow widths, proportional gap bars, correct two- and four-column button borders, forced-color endpoint distinctions, and explicit limits around tonic, spelling, function, emotion, and quality. Production-browser review confirms the v1.90 label; the unchanged-pitch Mixolydian reinterpretation; the one-sharp fifth window; the `+2` whole-tone collection, six-move Do loop, and two-component clock partition; complete nonvisual state descriptions; correct two-column mobile control geometry; desktop and 320-pixel layouts with no horizontal overflow; and no warning or error logs. Final gates pass full lint, type checking, 234 deterministic tests plus both rendered-page checks (236 total), and the GitHub Pages production export. The client-chunk advisory above 500 kB remains a later code-splitting optimization. These checks do not validate physical audio output, acoustic tuning or spectra, assistive-technology announcements, historical explanation, universal tonal hearing, learner comprehension, listener interpretation, or learning effectiveness.

The v1.91 Musical Edge Lenses fixtures verify an unanchored median-gap ruler without pulses-per-minute or phase claims, exact learner-declared 500 ms / 120 pulses-per-minute landmarks, signed five-note interval paths, selected-route coverage, catalog ambiguity, transposition invariance, interval-size recurrence, and explicit refusal to isolate a melody when one onset group contains a bouquet. Source contracts require three named edge regions, one concise delayed atomic live summary, an accessible independent-cue ring, a learner-pulse/local-ruler boundary, exact gap deviation rather than accuracy, separate roughness/repose/selected-Do change cues, listener-only felt character, compatible-route language, a single center annotation, 900- and 620-pixel reflow, 320-pixel no-overflow support, and non-color forced-color ring distinctions. Production-browser review covers the empty state, a five-note ascending route, and a two-field C-major-to-D-minor grouped change: the latter exposes `3 · 4 · 7` semitone pairs, `3–4` adjacent gaps, `↑5 / ↓0` nearest-key travel, independent 14% roughness / 70% fusion / 100% rising cues, a selected-Do-pull change, and a bouquet refusal without emitting a resolution or emotional verdict. Desktop, 390-pixel, and 320-pixel layout reads report no document or lens overflow, with no warning or error logs. Final gates pass full lint, type checking, the production build, 238 deterministic checks, and both rendered-page checks (240 total); the existing client-chunk advisory remains unchanged. These checks do not validate physical MIDI timing, acoustic spectrum, perceived fusion, performed fingering, intended meter, intended melody, tonal hearing, felt resolution, emotion, comprehension, aesthetic response, or learning effectiveness.

The v1.92 Semitone-First Interval Lens fixtures name every zero-through-twelve key spacing, preserve signed compound motion beyond one octave, keep physical distance separate from folded pitch-class distance, and distinguish isolated sequential notes from chronological attacks inside or after a close-time bouquet. Release and attack-snapshot evidence gates every simultaneous pair-sound estimate; missing overlap remains unknown. Source contracts require the signed semitone count to lead the lower-left lens, exact endpoints and recent paths, A4=440 12-TET multipliers and authored ratio landmarks, selected-frame role context, listening prompts framed as possibilities rather than emotional predictions, an atomic nonvisual update, forced-color support, and semitone-first mobile ordering. Production-browser review confirms isolated `↑3`, compound `↑13`, and bouquet-contained `↑4` cases; the latest spacing, direction, ratios, onset gap, route roles, and overlap provenance remain legible without duplicating the center graphic. Desktop and 320-pixel geometry report no document, stage, lens, or context-strip overflow, and browser warning/error logs are empty. Final gates pass full lint, type checking, the production build, and all 244 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate equal-key arithmetic, declared 12-TET reference coordinates, conservative simultaneity gating, accessibility contracts, and responsive layout—not measured acoustic pitch, universal interval emotion, consonance, tonal function, intended melody, listener preference, comprehension, or learning effectiveness.

The v1.93 Legibility and Declared Meter fixtures group close attacks before projecting them onto a learner-supplied two-bar grid, preserve exact nearest-pulse offsets, and clamp malformed tempo, meter, and grouping inputs without manufacturing a score. Source contracts require explicit lock/unlock and metronome controls, a beat-free unlocked onset strip, a named declared grid, one persistent three-mark fifths key, a promoted exact chord name, and explicit scale-frame names for a complete singleton five-attack span. Production-browser review confirms the former central density rings, visit seeds, stars, and satellite dots are absent; the remaining crown, attack circles, sounding outlines, semitone horizon, membranes, and fibers stay visually distinct. A declared 96 BPM three-pulse bar locks and unlocks correctly, the grid exposes its supplied-coordinate boundary, a grouped Do–Mi–Sol field promotes “Do major” above `3 · 4 · 7` semitone evidence, and a spaced Do–Re–Mi–Fa–Sol line names two compatible major-scale containers without asserting key. Desktop geometry keeps the 880-pixel edge stage and all three panels un-clipped; 320-pixel geometry stacks the graph, semitone path, rhythm, harmony, visual key, and interval context with no horizontal overflow. Browser warning/error logs are empty. Final gates pass lint, type checking, the production build, and all 245 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate declared-coordinate arithmetic, control state, template matching, catalog compatibility, accessibility contracts, and responsive presentation—not audible click output in every browser/device, inferred meter, rhythm intention, timing quality, detected key, chord function, listener emotion, comprehension, or learning effectiveness.

The v1.94 Voice Match and Interval Singing fixtures detect monophonic synthetic fundamentals, retain a harmonic-rich signal's lower fundamental instead of its stronger octave, and withhold estimates for silence, weak input, malformed windows, and noise-like alternation. Source contracts require learner-initiated microphone access, local analyser-only routing with no recording, upload, storage, or output connection, complete media-track shutdown, twenty-five signed semitone targets from −12 through +12, A4=440 target and nearest-key references, signed cents and hertz evidence, a text-redundant continuous target rail, and explicit model boundaries. Production-browser review confirms the idle panel creates no pitch marker and requests no permission; choosing +7 semitones maps Do at 261.6 Hz to Sol at 392.0 Hz, and an on-screen A4 attack re-anchors the same interval to 440.0 Hz and 659.3 Hz. At 1280 pixels the panel has no internal or document overflow; at 320 pixels its controls, readings, rail, and footer stack into one column with no horizontal overflow. Browser warning/error logs are empty. Final gates pass lint, type checking, the production build, and all 249 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate reference-coordinate arithmetic, detector behavior on controlled fixtures, local resource lifecycle, accessibility contracts, and responsive presentation—not microphone-hardware compatibility, room noise, browser/device latency, audible reference output, measured piano tuning, vocal timbre or quality, pitch correctness, harmony, emotion, listener response, comprehension, or learning effectiveness.

The v1.95 Visible Route Selection source contracts require a persistent named route region, native direct selector, selected route name, ordered semitone-gap loop, cumulative octave positions from Do, explicit locked-versus-following-evidence state, three written selection methods, a fifths-compass boundary, responsive one- and two-column layouts, and forced-color support. Production-browser review begins with the bright route following evidence, directly selects the shadow route while preserving Do, verifies its `2–1–2–2–1–2–2` gap loop and `0 · 2 · 3 · 5 · 7 · 8 · 10` positions, observes the frame lock, and confirms `pianoDo=0&pianoScale=shadow-seven` in the URL. Unlocking the frame removes both coordinates and returns to evidence-led bright-route fallback in the empty phrase. At 1280 pixels the route strip uses three columns with no section or document overflow; at 320 pixels it and its method list stack into one column with no overflowing descendants. Browser warning/error logs are empty. Final gates pass lint, type checking, the production build, and all 249 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate frame state transitions, equal-key route arithmetic, URL persistence, accessibility contracts, and responsive presentation—not key detection, tonal intention, acoustic tuning, theoretical correctness, emotional meaning, musical value, comprehension, or learning effectiveness.

The v1.96 Inversion-Aware Chord Names fixtures verify root position, first inversion, and second inversion for transposed F-major triads plus third inversion for G7, preventing absolute pitch-class sorting from changing the root-relative bass index. Source contracts require inversion in the bold upper-right exact chord name, a written bass-over-root explanation, and an explicit root-position root/bass statement. Production-browser review performs E–G–C as one grouped field and confirms `Do major · first inversion`, `Mi is the bass over Do root`, the `3 · 5 · 8` semitone pair evidence, and separate onset-togetherness/model cues. At 320 pixels the 252-pixel harmony lens and chord-name block have matching client and scroll widths, and the document remains 320 pixels wide. Browser warning/error logs are empty. Final gates pass lint, type checking, the production build, and all 249 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate catalog identity, root-relative inversion arithmetic, visible bass/root provenance, accessibility contracts, and responsive presentation—not harmonic function, correctness, stability, resolution, acoustic tuning, listener emotion, musical value, comprehension, or learning effectiveness.

The v1.97 Compact Chord Fingering fixtures verify root, first-inversion, and second-inversion triads; root and first-inversion four-note chords; and refusal for unavailable or wider-than-octave one-hand readings. Source contracts require a named right-hand guide, visible thumb/pinky endpoints, a complete chord-and-note-to-finger SVG alternative, a visible contextual boundary, and forced-color distinctions. Production-browser review performs Do–Mi–Sol and confirms a small realized keyboard with `1–3–5` markers below `Do major`, exact per-note alternative text, and no console warnings or errors. At 320 pixels the document remains 320 pixels wide; the 252-pixel harmony region and fingering graphic have equal client and scroll widths, while the SVG is 220 pixels wide. Final gates pass lint, type checking, the production build, and all 250 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate the bounded fingering table, realized-key spacing, accessible labeling, forced-color contracts, and responsive presentation—not the fingering a learner performed, left-hand allocation, hand comfort, ergonomic safety, pianistic correctness, phrase-level suitability, teaching efficacy, or musical quality.

The v1.98 Play-to-Set Movable Do source contracts require one persistent native pressed-state action, a polite armed/cancel status, MIDI and on-screen attack capture, selected-route retention, URL persistence, stale-exercise resets, forced-color support, and wording that makes the learner assignment distinct from detection. Production-browser review opens the Scales focus from the empty C-default frame, arms the action, plays on-screen D4, and confirms the same physical 293.7 Hz key becomes Do, the frame locks, the bright route remains selected, and `pianoDo=2&pianoScale=bright-seven` enters the URL. Switching to Intervals preserves both the D Do reference and control. At 320 pixels the document remains 320 pixels wide; the 282-pixel route region and 244-pixel action have matching client and scroll widths. Browser warning/error logs are empty. Final gates pass lint, type checking, the production build, and all 250 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate explicit state transitions, pitch-class folding, shared contextual propagation, persistence, accessibility contracts, and responsive layout—not detected tonic, heard key, harmonic function, tonal correctness, learner intention beyond the explicit action, comprehension, or learning effectiveness.

The v1.99 Register-Aware Chord Hands fixtures verify a low root-position triad, low second inversion, low four-note field, an upper-register triad, and a chord crossing middle C whose physical midpoint decides the hand. Source contracts require both hand labels, the visible middle-C rule, thumb/pinky endpoints, hand-specific finger order, a complete chord/register/note-to-finger SVG alternative, and explicit crossing/accompaniment caveats. Production-browser review performs C3–E3–G3 and confirms `Suggested left hand` with `5–3–1`, then performs C4–E4–G4 and confirms `Suggested right hand` with `1–3–5`; the right-hand SVG alternative repeats the chord, register cause, and every assignment. At 320 pixels the document remains 320 pixels wide; the 252-pixel harmony region and fingering graphic have matching client and scroll widths, while the keyboard SVG remains 220 pixels wide. Browser warning/error logs are empty. Final gates pass lint, type checking, the production build, and all 251 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate one transparent register heuristic, bounded fingering tables, realized-key spacing, accessible labeling, forced-color contracts, and responsive layout—not the hand or fingering performed, ergonomic safety, fixed hand territories, phrase-level suitability, pianistic correctness, teaching efficacy, or musical quality.

The v2.00 Standalone Voice Pitch Lab source contracts require a top-level Voice route, an independent chromatic A4=440 anchor selector, explicit pitch-not-speech wording, complete signed one-octave interval choices, and a microphone-activity meter separate from accepted periodic pitch. They also require that Piano no longer mount the microphone coach. Production-browser review changes the Voice anchor from C4 to A4 and confirms that the current anchor, target reference, and coach all update to 440.0 hertz while the shareable `lab=voice` state remains selected; returning to Piano confirms that neither the microphone start action nor the Voice Match panel is present. At 320 pixels the document remains exactly 320 pixels wide, and the Voice lab, reference controls, coach, and input-activity field all have matching client and scroll widths. Browser warning/error logs contain no warnings or errors. Real microphone permission and hardware pitch capture remain an explicit device-level check. Final gates pass lint, type checking, the production build, and all 251 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate route isolation, reference propagation, input-versus-pitch diagnosis, accessible labeling, and responsive layout—not microphone hardware compatibility, speech recognition, vocal health or quality, correctness, acoustic piano tuning, teaching efficacy, or musical value.

The v2.01 Immersion Scale Lens source contracts require one named lower-right region, twelve equal semitone cells, written selected-route and gap-loop state, and non-color-only recent, latest, and sounding distinctions with a complete image summary. Production-browser review first confirms twelve cells and seven bright-route positions, then performs Do–Re–Mi–Fa–Sol on the silent keyboard and confirms five recent markers, one latest marker, Sol at `+7 st from Do · route degree 5`, and retained coverage `5/7`. On the desktop, the harmony and scale regions do not overlap and the stage expands to contain both tall edge rows instead of clipping them. At 900 pixels the Scale Lens sits below Harmony and to the right of Semitone Path in a four-quadrant HUD; the document remains exactly 900 pixels wide. At 320 pixels the document remains exactly 320 pixels wide, the 252-pixel Scale Lens has matching client and scroll widths, and its 250-pixel twelve-cell grid has matching client and scroll widths. Browser warning/error logs are empty. Final gates pass lint, type checking, the production build, and all 251 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate selected-route propagation, semitone-cell geometry, performed-state markers, accessible labeling, non-clipping desktop growth, and responsive layout—not detected key, mode, function, correctness, stability, resolution, emotion, preference, musical value, comprehension, or learning effectiveness.

The v2.02 Quick HUD Scale Reset source contracts require two native pressed-state actions in the lower-right Scale Lens: one assigns the next new attack as movable Do, while the other starts a fresh evidence boundary for learning a scale frame from subsequently played notes. The contracts also require mutual cancellation, URL unpinning for frame discovery, a polite `0/4` distinct-note progress status, and retention of the previous frame only as provisional context until new evidence can stabilize. Production-browser review assigns D4 as Do and confirms `pianoDo=2&pianoScale=bright-seven`; starting frame discovery removes both coordinates, reports `0/4`, and ignores all earlier attacks. Four new distinct notes advance the status to four and move the discovering reference to 196.0 hertz with several compatible frames; restarting returns to `0/4`, and arming Do cancels frame learning. At 320 pixels the document remains exactly 320 pixels wide; the 252-pixel Scale Lens and action grid have matching client and scroll widths, and each action remains about 124 pixels wide without internal overflow. Browser warning/error logs are empty. Final gates pass lint, type checking, the production build, and all 251 automated checks including both rendered-page checks; the existing client-chunk advisory remains unchanged. These checks validate explicit learner actions, post-reset evidence boundaries, shared frame propagation, URL state, accessibility contracts, and responsive layout—not detected key, musical intention, tonal correctness, scale membership certainty, comprehension, or learning effectiveness.

The v2.03 Register-Aware Scale Fingering fixtures verify lower- and upper-register major routes, plus compact five- and six-position routes, with deterministic ascending suggestions and an explicit middle-C hand boundary. Source contracts require a small Scale Lens strip, a complete route-syllable-to-finger alternative, a written crossing cue, forced-color support, and visible wording that black-key layout, direction, hand size, and the surrounding passage may favor another fingering. Production-browser review performs lower-register Do and confirms `Suggested left hand` with `5–4–3–2–1–3–2–1` and finger 3 crossing after route degree 5, then performs an upper-register route and confirms `Suggested right hand` with `1–2–3–1–2–3–4–5` and the thumb crossing after route degree 3. The strip stays inside the existing lower-right Scale Lens, while its auto-fit cells retain a compact 22-pixel minimum for narrow layouts. Browser warning/error logs contain no warnings or errors. Final gates pass lint, type checking, the production Pages build, all 250 standard checks, and both rendered-page checks (252 total); the existing client-chunk advisory remains unchanged. These checks validate a transparent register heuristic, route-length fingering tables, accessible labeling, and responsive source contracts—not the hand or fingering performed, black-key-specific exceptions, descending fingering, hand comfort, ergonomic safety, phrase-level suitability, pianistic correctness, teaching efficacy, or musical quality.

The v2.04 Shared Short HUD History fixtures pin the newest twelve attacks as one deterministic Immersion slice and require the note trail, pitch-class visit counts, local center/recurrence evidence, relationship-window evidence, and chord grouping to consume that same slice. Source contracts require the visible `shared HUD history · notes + chords` count, the same sliced prop for Resonance Sky, and four visible membranes only as a rendering cap after shared-window chord grouping. Production-browser review begins at `0/12`, performs thirteen silent attacks, and confirms `12/12`, exactly twelve central attack marks, and a dynamic description stating that chord groups use the same twelve-attack history. With the 320-millisecond grouping option, three more attacks produce four bounded chord membranes while the shared count and note marks remain twelve. Browser warning/error logs contain no warnings or errors. Final gates pass lint, type checking, the production Pages build, all 250 standard checks, and both rendered-page checks (252 total); the existing client-chunk advisory remains unchanged. These checks validate deterministic local aging, a common evidence boundary, and accessible disclosure—not a detected phrase, musical memory span, chord importance, salience, meter, harmony, correctness, quality, comprehension, or learning effectiveness.

The v2.05 Research HUD fixtures pin twelve unique semitone-clock positions, transposition-invariant geometry, directed and shortest signed pitch-class distance, every generator loop from `+1` through `+7`, the complete `+7` fifths orbit, exact `+3`, `+4`, and diagonal `+7` thirds-lattice axes, and all 144 directed interval-table cells. Source contracts require three native arrangement controls, a fixed chromatic clock, selectable generator overlays with visible `12 ÷ gcd(12, step)` loop arithmetic, shared twelve-attack evidence, an unfolded physical semitone transition, named show/hide boundaries, and explicit limits on circle-of-fifths meaning. Production-browser review confirms `+2` creates two six-position loops without moving the twelve nodes, the lattice and 12-by-12 table remain complete, and silent C4→E4 input produces `2/12 shared attacks` plus an unfolded `+4 semitones · up` reading that survives switching between Immersion and Research HUD and reloading the URL-addressed Research focus. At 320 pixels the document remains exactly 320 pixels wide in both clock and all-pairs views, the focus and arrangement controls reflow without clipping, and browser warning/error logs are empty. Final gates pass lint, type checking, the production Pages build, all 258 standard checks, and both rendered-page checks (260 total); the existing client-chunk advisory remains unchanged. These checks validate 12-TET modular arithmetic, fixed spatial encodings, exact event sharing, accessibility contracts, and responsive presentation—not acoustic tuning, psychoacoustic affinity, tonal hierarchy, harmonic function, resolution, emotion, correctness, musical value, comprehension, or learning effectiveness.

Source-level accessibility contracts require a named piano region, native on-screen key and event buttons, polite live status, named SVG summaries, a text-labeled fifths cycle, forced-color states, and no positive tab indices. Browser review and physical MIDI-device testing remain separate release checks.

## Playback-level invariants

Synthesized labs share a nominal 0.065 master gain, equal-power simultaneous mixing, RMS-matched harmonic waveforms, a 0.24 coherent-peak ceiling, and one safety-compressor configuration. Recording playback is attenuated against both a 0.045 RMS target and a 0.20 peak ceiling. Tests cover silent and invalid inputs, voice-count invariance, timbre power matching, and recording gain bounds. Rhythm remains a separately calibrated transient, and authored musical dynamics remain intentionally audible.

## Open validation work

- Calibrate auditory filters and level-dependent loudness against suitable published conditions.
- Validate mixed-tone descriptors against expert annotations without treating agreement as musical truth.
- Run preregistered listening and comprehension studies with listener reports separated from predictions.
- Profile additional long-file formats and real audio devices beyond the generated performance budget.

## Performance budgets

The automated suite analyzes a generated two-minute, 12 kHz fixture and requires completion within five seconds, less than 256 MB of heap growth, and bounded multi-resolution frame counts. Recording Lab additionally exposes device-local worker throughput, browser-reported audio scheduling latency, median display-frame duration, and JavaScript heap when the browser provides it. These diagnostics do not replace real-device testing.
